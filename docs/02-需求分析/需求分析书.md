# 文档B：需求分析书（V6.7.1）

## 📌 Changelog

### 🟡 Patch — 2026-06-08（V6.7 → V6.7.1 子节编号归一化）

- **Change type**: 编号修正
- **Summary**: V6.7 顶层重排 16/17 章时只改了 `### 16.x/17.x`，遗漏了内部 `#### 15.10-15.19` 旧编号未迁移，导致 16.2 章内仍写"15.11.1 字段定义"这种逻辑矛盾
- **Impact**: 16/17 章内部 40 处子节编号统一归一
- **修复内容**:
  - 16.1 应收单：4 子节（15.10.1-4 → 16.1.1-4）
  - 16.2 收款单：4 子节（15.11.1-4 → 16.2.1-4）
  - 16.3 核销管理：5 子节（15.12.1-5 → 16.3.1-5）
  - 16.4 预收款：3 子节（15.13.1-3 → 16.4.1-3）
  - 16.5 坏账：5 子节（15.14.1-5 → 16.5.1-5）
  - 17.1 应付单：4 子节（15.15.1-4 → 17.1.1-4）
  - 17.2 付款单：3 子节（15.16.1-3 → 17.2.1-3）
  - 17.3 采购退货：3 子节（15.17.1-3 → 17.3.1-3）
  - 17.4 预付款：3 子节（15.18.1-3 → 17.4.1-3）
  - 17.5 现金折扣：6 子节（15.19.1-6 → 17.5.1-6）

### 🟡 Patch — 2026-06-08（V6.6 → V6.7 章节结构重排）

- **Change type**: 章节结构修正
- **Summary**: V6.4/V6.5 时期把"销售到收款"（15.10-15.14）和"采购到付款"（15.15-15.19）子节错误地追加在"第十五章-其他已实现模块"内，违反了"业务循环独立成章"的结构原则。V6.7 抽离为独立两章
- **Impact**: 章节重排 + 子节编号顺移
- **重排规则**:
  - 第十五章 15.1-15.9「其他已实现模块」保持原意（固定资产/工资/预算/成本/档案/辅助核算/系统设置/审计日志/预警管理）
  - **第十六章 销售到收款循环**（新）：16.1 应收单 + 16.2 收款单 + 16.3 核销管理 + 16.4 预收款 + 16.5 坏账计提与核销（原 15.10-15.14）
  - **第十七章 采购到付款循环**（新）：17.1 应付单 + 17.2 付款单 + 17.3 采购退货 + 17.4 预付款 + 17.5 现金折扣（原 15.15-15.19）
  - 原"十六、非功能需求"→"十八、非功能需求"
  - 原"十七、远期规划"→"十九、远期规划"
- **附录调整**: 附 A.10（V6.5 时期"P0 计划"）保持原状；新增**附 A.11 采购端 P0 任务完成状态清单**（V6.7 更新，区分计划 vs 完成两个视角）

### 🟡 Patch — 2026-06-08（V6.5 → V6.6 现金折扣模块完成）

- **Change type**: 需求实现状态更新
- **Summary**: TASK-3.4 现金折扣模块从 P0 缺失 → ✅ **P0 已实现并端到端测试通过**；补全附录 A.10 采购端 P0 任务清单
- **Impact**: 15.19 现状表/优先级表 + 附录 A 末尾追加 A.10
- **Details**:
  - 15.19.1 业务场景加 ✅ 已实现标记
  - 15.19.5 现状表 4 行从 P0 缺失 → ✅ 已实现
  - 15.19.6 优先级表 4 项 P0 → ✅，2 项 P1 仍待开发
  - **附录 A.10 采购端 P0 任务清单**（TASK-3.1 ~ TASK-3.6 + TASK-3.4 现金折扣）
- **端到端验证**（2026-06-08 03:25）:
  - HTTP `POST /api/v1/payment-entries/.../allocate` body `{allocated_amount: "1030.00", discount_amount: "100.00"}` → **HTTP 200**
  - DB 副产物：cash_discounts 1 条（rate=0.088496）+ cash_discount_vouchers 1 条 + journal_entries 1 张 + 3-line 凭证
  - 凭证规则：借 1122 应收账款 1130 / 贷 1002 银行存款 1030 / 贷 5602 财务费用 100（借贷平衡 1130=1130）
  - voucher_no 前缀 "CD-"（区别于普通 PZ- 前缀）

### 🟡 Patch — 2026-06-08（V6.4 → V6.5 补全采购到付款循环）
- **Change type**: 需求补充 + 现状对比
- **Summary**: 基于用户提供的《"采购到付款"循环需求分析报告 V1.0》，与销售到收款对称，新增应付单/付款单/采购退货/预付款/现金折扣 5 大模块；附录 B 给出采购端现状对比表
- **Impact**: 第十五章从 14 子节扩展到 19 子节（销售端 14 + 采购端 5）
- **Details**:
  - 15.15 应付单详细需求（核心债务载体，对应 V1.0 §3.2）
  - 15.16 付款单详细需求（采购端视角，对应 V1.0 §3.3）
  - 15.17 采购退货流程详细需求（对应 V1.0 §3.5）
  - 15.18 预付款管理详细需求（对应 V1.0 §3.6）
  - 15.19 现金折扣详细需求（对应 V1.0 §3.7，**采购端独有 P1**）
  - 附录 B：Go 版 vs 采购到付款 V1.0 现状对比表（8 张子表）
  - 附录 A 末尾追加"采购端 P0 任务清单"
- **明确归 V2 远期（不写入 V6.5）**:
  - 采购订单/入库单模型（暂估-冲回流程）
  - 多币种/汇率（应付单 currency / exchange_rate 字段）
  - 跨币种预付汇率差异处理
- **仍待补全**（P0）：应付单 handler 端点补全、应付单并发控制、应付单"已逾期"状态、现金折扣模型、预付款余额表、采购发票→应付单链路
- **来源**：用户提供《"采购到付款"循环需求分析报告 V1.0》（2026-06-07）

### 🟡 Patch — 2026-06-08（V6.3 → V6.4 补全销售到收款循环）
- **Change type**: 需求补充 + 现状对比
- **Summary**: 基于用户提供的《"销售到收款"循环需求分析报告 V1.0》，将应收单/收款单/核销/预收款/坏账 5 大模块同步到第十五章，新增 15.10-15.14 详细子节，附录 A 给出 Go 版实际实现与 V1.0 报告的差异对比
- **Impact**: 第十五章从"实现清单"升级为"需求 + 现状对照"双视图；为后续 P0 补全提供基准
- **Details**:
  - 15.2 工资核算：从 2 行升级为 15.2.1-15.2.5 详细子节
  - 15.10 应收单详细需求（对应 V1.0 §3.2 核心债权载体）
  - 15.11 收款单详细需求（对应 V1.0 §3.3）
  - 15.12 核销管理详细需求（对应 V1.0 §3.4 核心勾稽引擎）
  - 15.13 预收款管理详细需求（对应 V1.0 §3.6）
  - 15.14 坏账计提与核销详细需求（对应 V1.0 §3.7，P0 缺失）
  - 附录 A：Go 版 vs V1.0 报告 现状对比表
- **仍待补全**（P0）：坏账计提/核销、应收单"已逾期"状态、多币种/汇率、预收汇总报表
- **来源**：用户提供《"销售到收款"循环需求分析报告 V1.0》（2026-06-07）

### 🟡 Patch — 2026-06-08（V6.2 → V6.3 同步实现状态）
- **Change type**: 状态同步
- **Summary**: 第十三章（费用报销单）和第十四章（销售/进项发票）的所有 "MVP现状 vs 缺失" / "API 清单" / "开发优先级" 表格已与 Go 版实际代码同步
- **Impact**: 文档从 "未来规划导向" 转向 "现状+规划并重"，与代码实际状态对齐
- **Details**:
  - 13.1 报销单现状表：附件上传/驳回原因/发票关联标记为 ✅ 已修复（`feature/reimbursement-enhance` 分支）
  - 13.2.1 doc_no 格式：`FY{YYYYMM}-{NNNN}` → `RQ-YYYYMMDD-XXXX`（与代码实际一致）
  - 13.8 API 状态：17 个端点的实际实现状态全部刷新（带 API 路径前缀 `/api/v1/`）
  - 13.10 优先级：P0 三项已标注 ✅ 完成；P1 三项中 "费用类型细分" 仍为 ⚠️ 部分实现（5 类 vs 需求 8 类）
  - 14.1 总览表：进项发票独立模型/Excel 导入/OCR/验真四项标记为 ✅ 已超预期或 ⚠️ Mock 实现
  - 14.2.1 销售发票现状：数电票 10 字段/4 类发票/部分红冲标记为 ✅ 已修复
  - 14.3.1 进项发票现状：独立模型/3 步导入向导/OCR/验真 Mock 标记为 ✅/⚠️
  - 14.8 API 清单：19 个端点的实际状态全部刷新（`/invoices/sales/:id/part-red` 路径已对齐代码）
  - 14.10 优先级：6 项功能中 4 项已标注 ✅ 完成
- **Go 版实际进度**: 整体 ~90%（核心功能全可用，V2 优先级功能待开发）
- **仍未实现**: 进项发票真实 OCR/验真 API 集成、勾选抵扣、多级审批、预算控制、AI 增强

### 🔴 Major Change — 2026-06-08
- **Change type**: requirements addition
- **Summary**: 新增费用报销单详细需求章节（第十三章）+ 销售/进项发票详细需求章节（第十四章）
- **Impact**: Go版项目（/root/data/disk/huihua-finance）费用单据模块需求完整落地，作为后续开发依据
- **Details**:
  - 新增 13.1：MVP现状 vs 缺失对比（报销单）
  - 新增 13.2：字段设计（主表+费用类型细分）
  - 新增 13.3：审批流程（基础+MVP多级）
  - 新增 13.4：发票关联需求
  - 新增 13.5：附件管理需求
  - 新增 13.6：预算控制需求（V2）
  - 新增 13.7：凭证自动生成规则
  - 新增 13.8：API清单
  - 新增 13.9：前端页面清单
  - 新增 13.10：开发优先级
  - 新增 14.1：MVP现状总览（4类发票6维度矩阵）
  - 新增 14.2：销售发票详细需求（字段增强/数电票/部分红冲/凭证规则）
  - 新增 14.3：进项发票详细需求（独立InvoiceIn模型/验真/认证/抵扣规则）
  - 新增 14.4：进项发票Excel导入流程+预校验规则
  - 新增 14.5：进项发票OCR识别
  - 新增 14.6：发票验真集成
  - 新增 14.7：应收应付发票增强（核销记录/批量核销/核销凭证）
  - 新增 14.8：API清单
  - 新增 14.9：前端页面清单
  - 新增 14.10：开发优先级
  - 原第十章顺延为第十五章
  - 原第十一章顺延为第十六章
  - 原第十二章顺延为第十七章
- **Source**: 同步自 Python版项目需求分析书（V2.3，2026-06-07），由老丁确认

**文档定位：** 回答"用户要什么、场景规则是什么"，以项目实际已实现模块和规划为主线。

***

## 一、核心设计理念

| 理念             | 说明                                        |
| :------------- | :---------------------------------------- |
| **全渠道数据接入**    | 覆盖银行流水、进销项发票、费用单据、资产卡片、薪酬表等               |
| **Web 优先** | Web 端提供完整的财务操作闭环，Excel 批量导入支持 |
| **审核分离**       | 会计负责制单，财务经理负责审核               |
| **RBAC 权限**  | 基于角色的访问控制，支持 6 种角色（系统管理员、公司管理员、财务经理、会计、出纳、公司经理） |
| **自动凭证生成**  | 发票导入、税金计提审核等业务操作自动生成会计凭证 |
| **全操作可追溯**     | 所有操作记录不可篡改的操作日志                           |

***

## 二、用户角色定义

| 角色         | 后端 role_type    | 核心职责             | 使用频率   |
| :--------- | :---------------- | :--------------- | :----- |
| **系统管理员**   | `isSystemAdmin` | 系统级最高权限，绕过所有角色检查 | 低频     |
| **公司管理员**   | `setup` | 系统初始化、科目映射、用户管理 | 中频     |
| **财务经理**   | `finance` | 审批、税务管理、查看所有报表、跨读会计和出纳资源 | 高频     |
| **会计**     | `accounting` | 凭证录入、发票采集、工资核算、资产管理 | 工作日全天  |
| **出纳**     | `cash` | 资金收付、银行流水导入、日记账管理、银行对账、票据管理 | 每天多次   |
| **公司经理**   | `query` | 查看报表、凭证总览、业务单据审批视角，无录入权限 | 每天1-3次 |

### 2.1 角色权限矩阵

| 权限      | 系统管理员 | 公司管理员 | 财务经理    | 会计 | 出纳 | 公司经理 |
| :------ | :---: | :---: | :------ | :---: | :---: | :---: |
| 系统配置    | ✅ 全权 | ✅ 公司级 | ❌ | ❌ | ❌ | ❌ |
| 用户/角色管理 | | ✅ | ❌ | ❌ | ❌ | ❌ |
| 查看报表    | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ |
| 录入凭证草稿  | | ❌ | ✅ | ✅ | ❌ | ❌ |
| 审核/过账   | | ❌ | ✅ | ✅ | ❌ | ❌ |
| 录入银行流水  | | ❌ | ✅ | ✅ | ✅ | ❌ |
| 发票开具/领购 | | ❌ | ✅ | ✅ | ✅ | ❌ |
| 税务管理    | | ❌ | ✅ | ❌ | ❌ | ❌ |
| 工资/资产/预算/成本 | | ❌ | 审批 | ✅ | ❌ | ❌ |

***

## 三、模块一：数据采集（发票与流水导入）

### 3.1 模块定位

系统唯一数据入口，所有原始单据必须经过"接收→解析→归档"流程。

### 3.2 已实现功能清单

| 编号   | 功能              | 说明                                 | 状态   |
| :--- | :-------------- | :--------------------------------- | :--- |
| 1.1  | 发票 Excel 批量导入 | Web 端上传 Excel，两遍处理引擎自动解析（第一遍收集数据 → 第二遍按优先级处理 normal→redflush→voided→redchar） | ✅ 已实现 |
| 1.2  | 发票手工录入          | Web 表单直接填写发票号码、金额、日期、对方名称等全字段       | ✅ 已实现 |
| 1.3  | 发票查重            | 提交时自动校验发票号码是否重复，重复则阻断并提示         | ✅ 已实现 |
| 1.4  | 发票明细展示          | 一张发票包含多项商品/服务明细，详情弹窗展示明细表格       | ✅ 已实现 |
| 1.5  | 银行流水 Excel/CSV 导入 | Web 端拖拽上传，系统按预置模板解析列名，提取日期、金额、摘要、对方户名 | ✅ 已实现 |
| 1.6  | 银行流水手工录入        | Web 端表单逐笔录入银行交易                     | ✅ 已实现 |

### 3.3 银行流水驱动业财一体化增强

> **新增模块**。详细需求见 `docs/02-需求分析/业财一体化银行流水驱动模块需求分析.md`

在现有银行流水导入基础上，增强以下能力（V2.0定义共8个模块）：

| 编号 | 模块 | 功能 | 状态 | 备注 |
|:---|:---|:---|:---:|:---|
| 1.7 | 模块一：基础数据与规则引擎 | 银行账户/模板管理、智能分类规则库、科目映射 | ✅ 已实现 | 包含分类规则CRUD、默认规则初始化 |
| 1.8 | 模块二：银行流水导入与智能分类 | 六类分类（收/付/费用/利息/转账/待处理）、待处理流水池 | ⚠️ 部分实现 | 智能分类引擎已实现，待处理流水池待开发 |
| 1.9 | 模块三：业务单据管理 | 六种标准单据（新增银行转账单、现金单）、状态联动 | ✅ 已实现 | 收款单、付款单、报销单均已完成 |
| 1.10 | 模块四：发票核销管理 | 自动核销引擎、容差处理、手工核销中心、状态联动 | ⚠️ 部分实现 | 发票智能匹配已实现，容差处理待完善 |
| 1.11 | 模块五：银企对账 | 自动对账、余额调节表、手工强制对账 | ✅ 已实现 | 完整实现 |
| 1.12 | 模块六：现金管理 | 现金日记账、现金盘点 | ⚠️ 部分实现 | 日记账已实现，盘点待开发 |
| 1.13 | 模块七：凭证生成与总账接口 | 凭证模板、批量生成、双向联查、红蓝字冲销 | ✅ 已实现 | 完整实现 |
| 1.14 | 模块八：常用凭证模板 | 六种单据的标准借贷分录 | ⚠️ 部分实现 | 基础模板已实现，需完善 |

**关联关系：** 本增强模块对接 §3.1（数据采集）、§9 出纳管理（F10）、应收应付（F03/F04），向上为凭证管理（F01）和期末结账提供已核销的业务单据。

**用户角色：**
- 出纳：导入流水、智能分类核对、待处理流水池、现金盘点
- 应收/应付会计：发票核销管理
- 总账会计：凭证模板配置、凭证审核、月末结转
- 财务主管：现金单据审核、特殊业务审批

**当前进度：** 整体完成度约 75%，核心业务流程已贯通（流水导入→智能分类→单据生成→发票匹配→核销→凭证生成）。智能分类规则引擎已完成，下一步重点开发待处理流水池功能。

### 3.5 发票导入规则

**红冲识别规则（优先级）：**

| 优先级 | 判断条件 | 分类结果 |
|---|---|---|
| 1 | `价税合计 < 0` 或 `金额 < 0` | 红冲发票 |
| 2 | 状态列包含 "红冲" | 红冲发票 |
| 3 | 状态列包含 "作废" | 作废发票 |
| 4 | 以上都不是 | 正常发票 |

**两遍处理流程：**

1. **第一遍：数据收集** — 列名映射、行数据解析、状态分类、备注提取原发票号，将所有行数据收集到内存列表
2. **第二遍：按优先级排序处理** — 所有行按优先级排序（normal=0 → redflush=1 → voided=2 → redchar=3），单次循环中：
   - 优先创建正常发票（状态=issued，自动生成凭证）
   - 处理红冲发票时通过备注提取原发票号，匹配同批或历史正常发票并标记为 voided
   - 创建作废发票记录（状态=voided，不生成凭证）
   - 创建红字发票记录

**备注提取正则：** `r'被红冲蓝字(?:数电)?发票号码[：:]\s*(\d+)'`

**统一列名映射：** 系统内置 `COLUMN_ALIASES` 字典，支持多种 Excel 列名变体（如"发票号码"/"数电发票号"/"发票号"均可识别）

### 3.6 后台服务职责

| 工作       | 说明                  |
| :------- | :------------------ |
| 文件存储     | 原始文件存入存储系统    |
| 单据记录写入   | 解析后的发票、流水数据写入对应表 |
| 发票号查重    | 唯一性校验，重复则阻断 |
| 红冲关联匹配   | 四级匹配策略关联原发票 |
| 凭证自动生成   | 正常发票导入后自动生成借/贷分录 |

***

## 四、模块二：凭证处理（审核与过账）

### 4.1 模块定位

将原始单据转化为标准记账凭证的核心环节。Web 端提供完整的凭证管理功能：手工录入、审核工作台、批量审核、冲红账、反审核。

### 4.2 已实现功能清单

| 编号   | 功能        | 说明                            | 状态   |
| :--- | :-------- | :---------------------------- | :--- |
| 2.1  | 手工凭证录入    | 用户选择科目、填写金额、摘要，生成凭证草稿         | ✅ 已实现 |
| 2.2  | 凭证提交审核    | 会计将凭证草稿提交至"待审核"队列             | ✅ 已实现 |
| 2.3  | 审核工作台     | 分屏查看原始单据与凭证编辑界面 | ✅ 已实现 |
| 2.4  | 审核通过      | 财务经理审核通过，凭证进入"已审核"状态          | ✅ 已实现 |
| 2.5  | 审核驳回      | 财务经理驳回凭证，注明原因，退回至会计草稿         | ✅ 已实现 |
| 2.6  | 凭证过账      | 已审核凭证写入正式账套                   | ✅ 已实现 |
| 2.7  | 批量审核      | 财务经理勾选多张凭证一键通过审核              | ✅ 已实现 |
| 2.8  | 冲红账       | 财务经理发起红字冲销                    | ✅ 已实现 |
| 2.9  | 反审核       | 财务经理将已审核未过账凭证退回草稿             | ✅ 已实现 |
| 2.10 | 反过账       | 已记账凭证回退到未记账状态                 | ✅ 已实现 |
| 2.11 | 辅助核算强制/可选 | 科目挂接部门、个人、项目、存货               | ✅ 已实现 |

### 4.3 审核链设计

**当前实现：**
- 会计可完成制单与审核（暂不强制角色分离）
- 财务经理拥有审核/过账/冲红/反审核权限
- 所有操作日志记录操作人

**状态流转：** 草稿 → 待审核 → 已审核 → 已过账 → 已冲销

### 4.4 冲红账规则

| 规则项   | 说明                              |
| :---- | :------------------------------ |
| 触发方式  | Web 端凭证详情页点击"冲红"按钮（仅财务经理可见）      |
| 必填项   | 冲红原因（下拉：记账错误/重复记账/金额错误/科目错误/其他） |
| 生成结果  | 系统自动生成与原凭证借贷方向相反、金额相同的红字凭证      |
| 状态    | 红字凭证为"待审核"，需财务经理再次审核过账          |
| 原凭证状态 | 标记为"已冲销"，保留在账套中，不可修改            |
| 日志记录  | 记录冲红操作人、时间、原因、原凭证号、红字凭证号        |

***

## 五、模块三：期末处理（结转与结账）

### 5.1 模块定位

负责月末/年末固定财务操作的自动化执行。Web 端手动触发结转与检查，支持试算平衡、结账、反结账。

### 5.2 已实现功能清单

| 编号  | 功能     | 说明                        | 状态   |
| :-- | :----- | :------------------------ | :--- |
| 3.1 | 期末检查   | 检查凭证过账状态、断号、未记账凭证         | ✅ 已实现 |
| 3.2 | 损益结转   | 收入/费用类科目余额自动转入本年利润，生成结转凭证 | ✅ 已实现 |
| 3.3 | 试算平衡检查 | 资产=负债+所有者权益校验             | ✅ 已实现 |
| 3.4 | 结账     | 锁定当期账期，生成下期空白账套           | ✅ 已实现 |
| 3.5 | 反结账    | 财务经理解锁账期                  | ✅ 已实现 |

### 5.3 试算平衡检查规则

| 检查项               | 不通过时处理             |
| :---------------- | :----------------- |
| 资产总计 ≠ 负债+所有者权益总计 | 阻断结账，页面高亮显示差异科目及金额 |
| 总账科目余额 ≠ 明细科目余额之和 | 标记差异科目，引导排查    |
| 存在未过账凭证           | 列表展示未过账凭证，禁止继续结账   |
| 存在断号凭证            | 提示警告，但可由财务经理确认后继续  |

***

## 六、模块四：税务管理

### 6.1 模块定位

贯穿全流程的税务风险管控。Web 端提供税种管理、税务申报、税负率查询、税金计提工作流。

### 6.2 已实现功能清单

| 编号  | 功能       | 说明                          | 状态   |
| :-- | :------- | :-------------------------- | :--- |
| 4.1  | 税种管理    | 税种增删改查、计税方法配置              | ✅ 已实现 |
| 4.2  | 税负率查询   | 根据账务数据计算增值税、所得税税负率，Web 仪表板展示 | ✅ 已实现 |
| 4.3  | 税金计提    | 按期间自动计算各税种应计提金额            | ✅ 已实现 |
| 4.4  | 税金计提审核  | 审核时自动生成会计凭证（借 6403 / 贷 2221）| ✅ 已实现 |
| 4.5  | 税金计提提交  | 提交审核工作流                     | ✅ 已实现 |
| 4.6  | 税务申报    | 申报表生成与管理                     | ✅ 已实现 |

### 6.3 税金计提工作流

```
选择期间 → 自动计算各税种计提金额 → 提交审核 → 财务经理审核通过
                                                        │
                                            自动生成凭证：
                                            借: 6403 税金及附加
                                            贷: 2221 应交税费
```

**共享计税服务层：** `backend/app/services/tax_calculation.py` 提供统一计算逻辑，消除代码重复。

**税负率日期计算：** 使用 `calendar.monthrange` 正确计算月末日期，避免硬编码 28 号 bug。

### 6.4 人工审核权

| 权限       | 拥有角色       | 操作       | 条件限制    |
| :------- | :--------- | :------- | :------ |
| 查看税负率    | 财务经理/会计/公司经理 | 仅展示数据    | —       |
| 审核税金计提   | 财务经理       | 审核通过/驳回 | 可修改申报数据 |
| 确认申报     | 财务经理       | 确认无误     | 需申报权限   |

***

## 七、模块五：结果输出（财务报表生成）

### 7.1 模块定位

数据层输出，所有需"导出交给外部"的报表。Web 端提供核心法定报表及账簿查询、导出。

### 7.2 已实现功能清单

| 编号   | 功能       | 说明                    | 状态   |
| :--- | :------- | :-------------------- | :--- |
| 5.1  | 资产负债表    | 期末/年初对比，支持导出 PDF/Excel | ✅ 已实现 |
| 5.2  | 利润表      | 本期/本年累计，环比同比          | ✅ 已实现 |
| 5.3  | 现金流量表    | 自动生成                     | ✅ 已实现 |
| 5.4  | 科目余额表    | 期初/本期/期末余额            | ✅ 已实现 |
| 5.5  | 总账       | 按科目、期间查询              | ✅ 已实现 |
| 5.6  | 明细账      | 支持穿透至凭证               | ✅ 已实现 |
| 5.7  | 报表导出     | PDF/Excel 格式下载         | ✅ 已实现 |

### 7.3 报表计算原则

- 所有报表数据直接从账套中计算，不经过任何 AI 处理
- 用户可在 Web 端按期间筛选，点击金额可下钻至明细凭证

### 7.4 人工权限

- 财务经理、会计、公司经理均可查看报表，出纳不可查看
- 报表为账务数据的投影，不可直接修改数字。发现异常需追溯到凭证层修正

***

## 八、模块六：智能查询与汇总中心

### 8.1 模块定位

连接基础核算与决策支持的数据中枢，负责高效、灵活地查询和汇总所有账务数据。

### 8.2 已实现功能清单

| 编号  | 功能      | 说明                               | 状态   |
| :-- | :------ | :------------------------------- | :--- |
| X.1 | 凭证明细查询  | 按科目、期间、金额范围、摘要关键词、制单人等多条件组合查询    | ✅ 已实现 |
| X.2 | 科目辅助余额表 | 按科目+辅助核算维度展示期初、本期、期末余额 | ✅ 已实现 |
| X.3 | 简易账龄分析表 | 按客户/供应商展示应收应付余额及账龄区间分布           | ✅ 已实现 |
| X.4 | 查询结果导出  | 支持查询结果导出为 Excel                   | ✅ 已实现 |

### 8.3 凭证明细查询（Web 端）

**查询条件：**

| 筛选字段    | 控件类型       | 说明                 |
| :------ | :--------- | :----------------- |
| 会计期间    | 下拉选择（年+月）  | 必选，默认当前期间          |
| 科目编码/名称 | 文本输入+科目选择器 | 支持精确匹配             |
| 摘要关键词   | 文本输入       | 精确子串匹配（SQL LIKE）   |
| 金额范围    | 最小值/最大值输入框 | 按借方或贷方金额筛选         |
| 凭证状态    | 多选框        | 草稿/待审核/已审核/已过账/已冲销 |
| 制单人     | 下拉选择       | 选择制单用户             |
| 凭证号     | 文本输入       | 精确或范围查询            |

***

## 九、模块七：出纳管理

### 9.1 模块定位

出纳角色的核心工作区域，覆盖日记账管理、银行对账、票据与资金管理、报表查询与分析、会计协同与凭证接口五大模块。

### 9.2 已实现功能清单

| 编号   | 功能              | 说明                                 | 状态   |
| :--- | :-------------- | :--------------------------------- | :--- |
| 9.1  | 日记账管理 | 现金日记账、银行存款日记账的录入与查询 | ✅ 已实现 |
| 9.2  | 银行流水导入 | Excel/CSV 导入银行流水，自动解析生成日记账 | ✅ 已实现 |
| 9.3  | 银行对账 | 银行流水与企业账目核对，生成余额调节表 | ✅ 已实现 |
| 9.4  | 收款单管理 | 收款单据录入、查询、审核 | ✅ 已实现 |
| 9.5  | 付款单管理 | 付款单据录入、查询、审核 | ✅ 已实现 |
| 9.6  | 费用报销单管理 | 报销单据录入、查询、审核 | ✅ 已实现 |
| 9.7  | 银行账户管理 | 银行账户增删改查 | ✅ 已实现 |
| 9.8  | 资金流水管理 | 资金流水查询、统计 | ✅ 已实现 |

### 9.3 出纳核心规则

**日记账规则：**
- 按日期顺序记录，支持现金/银行两种日记账
- 每日结出余额，与银行对账单核对

**银行对账规则：**
- 银行流水与企业日记账逐笔核对
- 标记已达/未达账项
- 生成银行余额调节表

**权限控制：**
- 出纳只能操作资金相关单据（收款/付款/报销/流水）
- 出纳不能查看报表（资产负债表/利润表等）
- 财务经理可审批出纳录入的单据

---

## 十三、费用报销单详细需求

> 费用报销单是会计日常高频操作之一，涵盖报销人填单→审批→生成凭证的全流程。

### 13.1 MVP现状 vs 缺失（V6.3 与 Go 版实际代码同步）

| 维度 | V6.2 文档描述 | V6.3 Go版实际 | 状态 |
|------|-------------|-------------|------|
| 数据模型 | ✅ 基本字段 | ✅ 基本字段（无明细行） | 与文档一致 |
| 费用类型 | ✅ 基础类型 | ✅ **5 类**（travel/office/transport/entertain/other），未细分到 8 类 | ⚠️ 部分实现 |
| 审批流程 | ✅ draft→pending→approved/rejected | ✅ 状态用 `docstatus` 整数（0/1/2/3） | 实现方式不同 |
| 附件 | ✅ attachment_count（仅数字） | ✅ **实际附件上传已实现**（`reimbursement_attachments` 表 + multipart API）| ✅ **已修复** |
| 发票关联 | ❌ 未关联 | ✅ **已实现**（`reimbursement_invoice_links` 表 + 关联 API）| ✅ **已修复** |
| 预算控制 | ❌ | ❌ 未实现 | 与文档一致（V2 规划）|
| 驳回原因 | ⚠️ API接受但未落地 | ✅ **已落地**（`reject_reason` 字段持久化，commit `1e57035d`）| ✅ **已修复** |
| 凭证生成 | ✅ | ✅ 需 6602.03 等管理费用科目种子数据 | 需初始化科目 |

### 13.2 字段设计

#### 13.2.1 主表（BusReimbursement）

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| doc_no | String(20) | 是 | 编号，**V6.3 实际格式：`RQ-YYYYMMDD-XXXX`**（代码实现，非文档的 `FY{YYYYMM}-{NNNN}`）|
| doc_date | Date | 是 | 报销日期 |
| expense_type | String(50) | 是 | 费用类型 |
| amount | Decimal(18,2) | 是 | 报销总额 |
| summary | String(500) | 否 | 报销事由 |
| dept_id | UUID | 否 | 部门ID |
| dept_name | String | 否 | 部门名称 |
| employee_id | UUID | 否 | 报销人ID |
| employee_name | String | 否 | 报销人姓名 |
| payee | String(100) | 否 | 收款人 |
| payment_method | String(20) | 否 | cash/bank/transfer/wechat/alipay |
| bank_account_id | UUID | 否 | 支付账户 |
| invoice_count | Integer | 否 | 关联发票张数 |
| attachment_count | Integer | 否 | 附件张数 |
| reject_reason | String(500) | 否 | 驳回原因（持久化存储） |
| approved_by | String(100) | 否 | 审批人 |
| approved_at | DateTime | 否 | 审批时间 |
| status | String(20) | 是 | draft/pending/approved/rejected |
| voucher_id | UUID | 否 | 已生成凭证ID |
| voucher_no | String(20) | 否 | 凭证号 |
| created_by | UUID | 是 | 创建人 |
| created_by_name | String(100) | 是 | 创建人名称 |
| created_at | DateTime | 是 | 创建时间 |
| updated_at | DateTime | 否 | 更新时间 |

#### 13.2.2 费用类型细分

| 类型代码 | 中文 | 可选子类型 |
|---------|------|----------|
| travel | 差旅费 | train（火车）/plane（机票）/hotel（住宿）/meal（餐费）/taxi（市内交通） |
| office | 办公费 | supply（办公用品）/print（打印复印）/post（快递） |
| entertain | 业务招待费 | meal（餐费）/gift（礼品）/other（其他） |
| transport | 交通费 | fuel（油费）/parking（停车）/toll（过路费）/bus（公交） |
| communication | 通讯费 | phone（电话）/mobile（手机）/internet（网络） |
| training | 培训费 | course（课程）/book（书籍）/exam（考试） |
| welfare | 福利费 | birthday（生日）/holiday（节日） |
| other | 其他 | — |

### 13.3 审批流程

#### 13.3.1 基础流程（MVP）

```
草稿(draft)
    ↓ [提交]
待审核(pending)
    ↓ [审核通过]      ↓ [驳回]
已通过(approved)    已驳回(rejected)
    ↓                    ↓
生成凭证           [重新编辑] → draft → 重新提交
```

#### 13.3.2 多级审批（V2）

| 级别 | 金额门槛 | 审批人 | 说明 |
|------|---------|--------|------|
| 一级 | 0~5,000元 | 部门主管 | 直接通过 |
| 二级 | 5,001~20,000元 | 部门主管+财务经理 | 需财务复核 |
| 三级 | >20,000元 | 部门主管+财务经理+总经理 | 最终审批 |

### 13.4 发票关联需求

| 功能 | 说明 |
|------|------|
| 关联进项发票 | 报销单可关联1~N张进项发票，关联后显示发票号+金额 |
| 发票OCR识别 | 扫描/拍照发票，自动提取：发票号/金额/日期/类型 |
| 验真 | 发票与国税系统比对（数电发票接口） |
| 部分报销 | 发票金额 > 报销金额时，允许部分报销 |
| 无票报销 | 允许无发票报销（需填写原因说明） |

### 13.5 附件管理需求

| 功能 | 说明 |
|------|------|
| 本地上传 | 支持 jpg/png/pdf，单文件≤10MB |
| 批量上传 | 一次可选多个文件 |
| 附件预览 | 图片/PDF在线预览 |
| 附件下载 | 打包下载（zip） |
| 存储路径 | `uploads/attachments/{doc_no}/` |

### 13.6 预算控制需求（V2）

| 功能 | 说明 |
|------|------|
| 部门预算 | 每个部门年度/季度费用预算 |
| 实时检查 | 报销单提交时，检查部门剩余预算 |
| 超预算预警 | 金额超过预算80%→黄色预警；超100%→红色阻止提交 |
| 预算调整 | 财务主管可调整部门预算 |

### 13.7 凭证自动生成规则

根据 `BusDocMapping` 科目映射配置：

| 费用类型 | 借方科目 | 贷方科目 |
|---------|---------|---------|
| 差旅费 travel | 管理费用-差旅费 | 银行存款/应付账款 |
| 办公费 office | 管理费用-办公费 | 银行存款/应付账款 |
| 业务招待费 entertain | 管理费用-业务招待费 | 银行存款 |
| 交通费 transport | 管理费用-交通费 | 银行存款 |
| 通讯费 communication | 管理费用-通讯费 | 银行存款 |
| 培训费 training | 管理费用-培训费 | 银行存款 |
| 福利费 welfare | 管理费用-福利费 | 银行存款 |
| 其他 other | 管理费用-其他 | 银行存款 |

### 13.8 API 清单（V6.3 已与 Go 版实际代码同步）

| 方法 | 路径 | 说明 | V6.3 实际状态 |
|------|------|------|------|
| POST | `/api/v1/reimbursements` | 创建 | ✅ 已有（字段名：`employee_name`/`amount` 是 string）|
| GET | `/api/v1/reimbursements` | 列表（分页+筛选） | ✅ 已有 |
| GET | `/api/v1/reimbursements/{id}` | 详情 | ✅ 已有 |
| PUT | `/api/v1/reimbursements/{id}` | 编辑（仅草稿） | ✅ 已有 |
| DELETE | `/api/v1/reimbursements/{id}` | 删除（仅草稿） | ✅ 已有 |
| POST | `/api/v1/reimbursements/{id}/submit` | 提交审核 | ✅ 已有 |
| POST | `/api/v1/reimbursements/{id}/approve` | 审核通过 | ✅ 已有（**测试需科目 6602.03 种子数据**）|
| POST | `/api/v1/reimbursements/{id}/reject` | 驳回（存原因） | ✅ **已修复**（reject_reason 字段已落地，commit `1e57035d`）|
| POST | `/api/v1/reimbursements/{id}/generate-voucher` | 生成凭证 | ✅ 已有 |
| GET | `/api/v1/reimbursements/{id}/attachments` | 获取附件列表 | ✅ **已实现**（`reimbursement_attachments` 表）|
| POST | `/api/v1/reimbursements/{id}/attachments` | 上传附件（multipart） | ✅ **已实现** |
| DELETE | `/api/v1/reimbursements/{id}/attachments/{file_id}` | 删除附件 | ✅ **已实现** |
| GET | `/api/v1/reimbursements/{id}/invoices` | 获取可关联发票 | ✅ **已实现**（`reimbursement_invoice_links` 表）|
| POST | `/api/v1/reimbursements/{id}/invoices/{invoice_id}` | 关联发票 | ✅ **已实现** |
| GET | `/api/v1/report/expense_summary` | 费用汇总报表 | ✅ 已有 |
| GET | `/api/v1/report/expense_by_dept` | 按部门费用报表 | ✅ 已有 |
| GET | `/api/v1/report/expense_by_type` | 按类型费用报表 | ❌ 新增（V2 规划）|

### 13.9 前端页面清单

| 页面 | 路由 | 功能 |
|------|------|------|
| 费用报销单列表 | `/business/reimbursements` | 列表+筛选+操作按钮 |
| 新增报销单 | `/business/reimbursements/create` | 表单（草稿） |
| 编辑报销单 | `/business/reimbursements/edit/:id` | 表单（草稿可编辑） |
| 报销单详情 | `/business/reimbursements/detail/:id` | 详情+附件+发票关联 |
| 附件管理 | 详情页Tab | 上传/预览/删除附件 |

### 13.10 开发优先级（V6.3 已完成项标注）

| 优先级 | 功能 | MVP必要性 | V6.3 状态 |
|--------|------|---------|---------|
| P0 | 附件实际上传（替代attachment_count仅数字） | 🔴 必要 | ✅ **已完成**（`feature/reimbursement-enhance`）|
| P0 | 驳回原因持久化存储到DB | 🔴 必要 | ✅ **已完成**（commit `1e57035d`）|
| P0 | 发票关联（进项发票） | 🔴 必要 | ✅ **已完成**（`reimbursement_invoice_links` 表）|
| P1 | 费用类型细分（子类型 8 类） | 🟡 推荐 | ⚠️ **部分实现**（仅 5 类，缺 communication/training/welfare）|
| P1 | 多级审批流程 | 🟡 推荐 | ❌ 未实现（V2 规划）|
| P1 | 按类型费用报表 | 🟡 推荐 | ❌ 未实现（V2 规划）|
| P2 | 预算控制 | 🟢 V2规划 | ❌ 未实现 |

---

## 十四、销售发票与进项发票详细需求

> 发票模块是财务闭环的核心节点：销售发票生成应收→收款核销；进项发票认证抵扣→付款核销。

### 14.1 MVP现状总览（V6.3 与 Go 版实际代码同步）

| 发票类型 | V6.2 文档 | V6.3 Go版实际 | 状态 |
|---------|---------|-------------|------|
| 销售发票（`SalesInvoice`） | ✅ 基础完整 | ✅ **已增强**（数电票 10 个新字段 + 部分红冲 + 整单红冲）| ✅ **已超预期** |
| 进项发票 | ❌ 无独立模型 | ✅ **`expense_invoices` 表已独立**（InvoiceIn 等价）| ✅ **已超预期** |
| 进项发票 Excel 导入 | ❌ 无 | ✅ **3 步向导已实现**（upload→preview→confirm）| ✅ **已超预期** |
| 进项发票 OCR | ❌ 无 | ⚠️ **Mock 实现**（真实 OCR API 待集成）| ⚠️ 部分实现 |
| 进项发票验真 | ❌ 无 | ⚠️ **Mock 实现**（真实国税 API 待集成）| ⚠️ 部分实现 |
| 应收发票（ArInvoice） | ✅ | ✅ 一单一应收（用户确认方案 A 保留）| 实现方式不同 |
| 应付发票（ApInvoice） | ✅ | ✅ 仅付款核销用 | 实现方式不同 |

### 14.2 销售发票（InvoiceIssue）详细需求

#### 14.2.1 现状 vs 缺失（V6.3 与 Go 版实际代码同步）

| 维度 | V6.2 文档 | V6.3 Go版实际 | 状态 |
|------|---------|-------------|------|
| 数据模型 | ✅ 基本字段 | ✅ **已增加 10 个数电票字段**（invoice_kind/electronic_url/red_letter_info_id/red_letter_reason/original_invoice_id/is_part_red/red_amount/tax_authority_code/confirm_status/confirm_date）| ✅ **已修复** |
| 发票类型 | ✅ special/normal | ✅ 4 类（paper_special/paper_normal/electronic_special/electronic_normal）| ✅ **已修复** |
| 红冲作废 | ✅ 同月作废/跨月红冲 | ✅ **部分红冲已实现**（`POST /invoices/sales/:id/part-red`）+ 整单红冲 | ✅ **已修复** |
| 批量操作 | ✅ 批量审核 | ✅ 批量审核（红冲/作废在详情页操作） | 与文档一致 |
| 导入 | ✅ Excel批量导入+预览 | ✅ Excel批量导入+预览 | 与文档一致 |
| OCR | ⚠️ 有接口但未集成到UI | ⚠️ 销售发票 OCR 仍待集成 | 与文档一致 |
| 凭证生成 | ✅ 基础生成 | ✅ 基础生成 | 与文档一致 |
| 报表统计 | ⚠️ 基础统计弹窗 | ⚠️ 基础统计弹窗 | 与文档一致 |

#### 14.2.2 字段设计（增强）

**现有 InvoiceIssue 字段保留，新增以下字段：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| invoice_kind | String(20) | 否 | 纸票/数电普票/数电专票 |
| electronic_url | String(500) | 否 | 数电发票版式文件URL |
| red_letter_info_id | String(50) | 否 | 红字信息表编号（关联开具） |
| red_letter_reason | String(200) | 否 | 开具红字发票原因 |
| original_invoice_id | String(36) | 否 | 原蓝字发票ID（红冲时填） |
| is_part_red | Boolean | 否 | 是否部分红冲 |
| red_amount | Decimal(18,2) | 否 | 红冲金额（部分红冲时填写） |
| tax_authority_code | String(20) | 否 | 主管税务机关代码 |
| confirm_status | String(20) | 否 | 确认状态：unconfirmed/confirmed/invalid |
| confirm_date | Date | 否 | 确认日期 |

#### 14.2.3 发票类型细分

| 类型代码 | 中文 | 说明 |
|---------|------|------|
| paper_special | 纸质增值税专用发票 | 传统纸票 |
| paper_normal | 纸质增值税普通发票 | 传统纸票 |
| electronic_special | 数电专用发票 | 电子专票 |
| electronic_normal | 数电普通发票 | 电子普票 |

#### 14.2.4 部分红冲规则

| 规则 | 说明 |
|------|------|
| 触发条件 | 原发票已收票/已认证，需部分红冲 |
| 前置要求 | 填红字信息表并审核通过 |
| 操作入口 | 发票详情页→红冲→选择"部分红冲" |
| 必填项 | 红冲金额（≤原发票金额）、红冲原因 |
| 生成结果 | 新增红字发票，is_part_red=true，red_amount=红冲金额 |
| 原发票状态 | 更新为 PART_RED（部分红冲） |

#### 14.2.5 凭证生成规则

| 发票类型 | 借方 | 贷方 | 说明 |
|---------|------|------|------|
| 正常销售发票 | 应收账款 | 主营业务收入 / 应交税费-销项税额 | 确认应收 |
| 红冲发票 | 主营业务收入（红字）/ 应交税费-销项税额（红字） | 应收账款（红字） | 减少应收 |
| 部分红冲 | 主营业务收入（红字部分）/ 应交税费-销项税额（红字部分） | 应收账款（红字部分） | 部分减少应收 |

### 14.3 进项发票详细需求

#### 14.3.1 现状 vs 缺失（V6.3 与 Go 版实际代码同步）

| 维度 | V6.2 文档 | V6.3 Go版实际 | 状态 |
|------|---------|-------------|------|
| 数据模型 | ✅ ApInvoice 基础完整 | ✅ **`expense_invoices` 表已独立**（独立 InvoiceIn 模型，PostgreSQL 表）| ✅ **已修复** |
| 导入 | ❌ 无独立导入流程 | ✅ **3 步导入向导已实现**（`POST /expense-invoices/import/upload`）| ✅ **已修复** |
| OCR | ❌ 无 | ⚠️ **Mock 实现已就绪**（`POST /expense-invoices/ocr`，待真实 API 集成）| ⚠️ 部分实现 |
| 验真 | ❌ 无 | ⚠️ **Mock 实现已就绪**（`POST /expense-invoices/:id/verify`，待国税 API）| ⚠️ 部分实现 |
| 认证抵扣（V2） | ❌ | ❌ 未实现 | 与文档一致（V2 规划）|
| 核销 | ✅ | ✅ 通过关联报销单实现 | 与文档一致 |

#### 14.3.2 独立 InvoiceIn 模型

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| invoice_no | String(50) | 是 | 发票号码（唯一） |
| invoice_code | String(20) | 否 | 发票代码 |
| invoice_date | Date | 是 | 开票日期 |
| invoice_kind | String(20) | 是 | 纸票/数电普票/数电专票 |
| tax_amount | Decimal(18,2) | 是 | 税额 |
| total_amount | Decimal(18,2) | 是 | 价税合计 |
| vendor_id | UUID | 否 | 供应商ID |
| vendor_name | String(200) | 否 | 供应商名称 |
| tax_id | String(50) | 否 | 供应商税号 |
| verify_status | String(20) | 否 | unverified/verified/invalid |
| verified_at | DateTime | 否 | 验真时间 |
| deduction_status | String(20) | 否 | undeducted/deducted |
| deducted_at | DateTime | 否 | 抵扣时间 |
| source_file | String(500) | 否 | 来源文件路径 |
| ocr_data | JSON | 否 | OCR原始数据 |
| status | String(20) | 是 | pending/verified/deducted/invalid |

#### 14.3.3 验真集成

| 功能 | 说明 |
|------|------|
| 单张验真 | 发票详情页→验真按钮→国税查验平台API |
| 批量验真 | 列表页勾选→批量验真→结果展示 |
| 验真结果 | 通过/不一致/查无此票/异常 |
| 异常处理 | 验真不通过→标记为invalid+记录原因 |

#### 14.3.4 勾选抵扣（V2）

| 功能 | 说明 |
|------|------|
| 抵扣确认 | 确认发票用于进项抵扣 |
| 抵扣统计 | 按月统计可抵扣税额 |
| 申报联动 | 抵扣数据对接税务申报 |

### 14.4 进项发票Excel导入流程

```
用户上传Excel → 系统解析 → 预校验（查重/格式） → 预览确认 → 正式入库
```

| 步骤 | 说明 |
|------|------|
| 1. 上传 | 支持 .xlsx/.xls，模板下载 |
| 2. 解析 | 读取列：发票代码/发票号码/开票日期/价税合计/税额/供应商名称 |
| 3. 预校验 | 查重（invoice_no已存在→标记duplicate）、格式校验（日期/金额格式） |
| 4. 预览 | 展示解析结果，错误行标红，用户可修正 |
| 5. 确认 | 点确认正式写入数据库 |

### 14.5 进项发票OCR识别

| 功能 | 说明 |
|------|------|
| 拍照上传 | 手机拍照/相册选择，图片压缩≤2MB |
| 识别内容 | 发票号码/金额/日期/类型/供应商 |
| 结果确认 | 识别后展示，用户确认后写入 |
| 支持格式 | jpg/png/pdf |

### 14.6 发票验真集成

| 功能 | 说明 |
|------|------|
| 验真接口 | 国税查验平台API（或第三方聚合） |
| 验真时机 | 发票入库后 / 用户手动触发 |
| 结果存储 | verify_status + verified_at + verify_result |

### 14.7 应收应付发票增强

| 功能 | 说明 |
|------|------|
| 核销记录 | 每笔核销写一条 record（type/payer/payer_bank/matched_amount） |
| 批量核销 | 勾选多个应收→合并收款单→一键核销 |
| 核销凭证 | 核销完成后自动生成凭证（借应收/贷应收） |

### 14.8 API 清单（V6.3 已与 Go 版实际代码同步）

| 方法 | 路径 | 说明 | V6.3 实际状态 |
|------|------|------|------|
| GET | `/api/v1/invoices/sales` | 销售发票列表 | ✅ 已有（修复了 `red_amount` NULL 问题）|
| POST | `/api/v1/invoices/sales/import` | 批量导入销售发票 | ✅ 已有 |
| GET | `/api/v1/invoices/sales/import/{batch_id}/preview` | 导入预览 | ✅ 已有 |
| POST | `/api/v1/invoices/sales/import/{batch_id}/confirm` | 确认导入 | ✅ 已有 |
| POST | `/api/v1/invoices/sales/{id}/confirm` | 确认销售发票 | ✅ 已有 |
| POST | `/api/v1/invoices/sales/{id}/red` | 红冲销售发票 | ✅ **已修复**（整单红冲）|
| POST | `/api/v1/invoices/sales/{id}/part-red` | 部分红冲 | ✅ **已实现** |
| POST | `/api/v1/invoices/sales/{id}/void` | 作废销售发票 | ✅ **已实现**（业务规则验证通过）|
| GET | `/api/v1/expense-invoices` | 采购（进项）发票列表 | ✅ **已实现**（`expense_invoices` 表）|
| POST | `/api/v1/expense-invoices/import/upload` | 批量导入采购发票 | ✅ **已实现**（3 步向导：upload→preview→confirm）|
| POST | `/api/v1/expense-invoices/ocr` | OCR识别采购发票 | ⚠️ **Mock 实现**（真实 API 待集成）|
| POST | `/api/v1/expense-invoices/{id}/verify` | 验真单张 | ⚠️ **Mock 实现**（返回模拟验真结果）|
| POST | `/api/v1/expense-invoices/verify/batch` | 批量验真 | ⚠️ **Mock 实现**（待国税 API）|
| POST | `/api/v1/expense-invoices/{id}/confirm` | 确认采购发票 | ✅ **已实现** |
| POST | `/api/v1/expense-invoices/{id}/deduct` | 勾选抵扣 | ❌ 未实现（V2 规划）|
| GET | `/api/v1/invoices/{id}/reconciliation` | 核销记录查询 | ✅ 已有 |
| GET | `/api/v1/report/invoice_summary` | 发票汇总报表 | ✅ 已有 |
| GET | `/api/v1/report/invoice_by_customer` | 按客户统计 | ❌ 未实现（V2 规划）|
| GET | `/api/v1/report/invoice_by_type` | 按类型统计 | ❌ 未实现（V2 规划）|

### 14.9 前端页面清单

| 页面 | 路由 | 功能 |
|------|------|------|
| 发票总览 | `/invoice` | Tab切换（销售/采购/费用），统计摘要 |
| 销售发票列表 | `/invoice/sales` | 列表+导入+审核 |
| 销售发票详情 | `/invoice/sales/detail/:id` | 详情+红冲+作废 |
| 采购发票列表 | `/invoice/purchase` | 列表+导入+OCR+验真 |
| 采购发票详情 | `/invoice/purchase/detail/:id` | 详情+验真+抵扣 |
| 发票导入向导 | 弹窗 | 上传→预览→确认三步 |

### 14.10 开发优先级（V6.3 已完成项标注）

| 优先级 | 功能 | MVP必要性 | V6.3 状态 |
|--------|------|---------|---------|
| P0 | 进项发票独立模型（InvoiceIn） | 🔴 必要 | ✅ **已完成**（`expense_invoices` 表）|
| P0 | 进项发票Excel导入 | 🔴 必要 | ✅ **已完成**（3 步向导）|
| P1 | 销售发票数电票字段增强 | 🟡 推荐 | ✅ **已完成**（10 个新字段）|
| P1 | 销售发票部分红冲 | 🟡 推荐 | ✅ **已完成**（`/part-red` API）|
| P1 | 进项发票OCR识别 | 🟡 推荐 | ⚠️ **Mock 已实现**（真实 API 待集成）|
| P1 | 进项发票验真集成 | 🟡 推荐 | ⚠️ **Mock 已实现**（国税 API 待集成）|
| P2 | 勾选抵扣工作流 | 🟢 V2规划 | ❌ 未实现 |

---

## 十五、其他已实现模块

### 15.1 固定资产管理

| 功能 | 说明 | 状态 |
|---|---|---|
| 资产卡片 | 登记资产名称、类别、原值、残值率、使用年限、开始使用日期 | ✅ 已实现 |
| 资产列表 | 按状态、类别查询资产 | ✅ 已实现 |
| 折旧计算 | 按设定方法计算折旧 | ✅ 已实现 |

### 15.2 工资核算（V6.4 升级为详细需求）

#### 15.2.1 工资单（Payroll）字段

参考 Go 版模型 `internal/model/payroll.go`：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| payroll_no | String(20) | 是 | 工资单号（唯一） |
| employee_name | String | 是 | 员工姓名 |
| department_name | String | 是 | 部门名称 |
| period_no | Int | 是 | 工资期间（YYYYMM 格式，如 202506）|
| gross_salary | Decimal | 是 | 应发工资（税前）|
| individual_tax | Decimal | 是 | 代扣个税 |
| social_security | Decimal | 是 | 代扣社保 |
| housing_fund | Decimal | 是 | 代扣公积金 |
| other_deductions | Decimal | 是 | 其他扣款 |
| net_salary | Decimal | 是 | 实发工资 = gross_salary - individual_tax - social_security - housing_fund - other_deductions |
| payment_date | Date | 是 | 发放日期 |
| bank_account_no | String | 是 | 收款银行卡号 |
| status | String(20) | 是 | draft/confirmed/posted |
| docstatus | Int | 是 | 0=草稿/1=已确认/2=已过账 |
| voucher_id | UUID? | 否 | 关联凭证ID |
| source | String(20) | 是 | manual/excel/import |
| remark | String(500)? | 否 | 备注 |

#### 15.2.2 工资计算规则

- **应发工资 (gross_salary) = 基本工资 + 绩效工资 + 各类补贴**
- **实发工资 (net_salary) = gross_salary - 各项代扣**
- 工资项目可配置（基本/绩效/工龄/通讯/交通等）
- 工资期间 (period_no) 按自然月，格式 YYYYMM

#### 15.2.3 凭证自动生成规则

按工资单提交时自动生成转账凭证：

```
借：应付职工薪酬—工资
  　　贷：银行存款 / 库存现金（实发 net_salary）
  　　贷：应交税费—应交个人所得税（代扣个税）
  　　贷：应付职工薪酬—社保（代扣社保）
  　　贷：应付职工薪酬—公积金（代扣公积金）
```

#### 15.2.4 API 清单（Go 版实际）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/payroll` | 工资单列表（分页+筛选） |
| POST | `/api/v1/payroll` | 创建工资单 |
| GET | `/api/v1/payroll/:id` | 工资单详情 |
| POST | `/api/v1/payroll/:id/submit` | 提交审核 |
| POST | `/api/v1/payroll/:id/approve` | 审核通过 |
| POST | `/api/v1/payroll/:id/generate-voucher` | 生成凭证 |

#### 15.2.5 开发优先级

| 优先级 | 功能 | 状态 |
|--------|------|------|
| P0 | 工资单手工录入 | ✅ 已实现 |
| P0 | 工资单提交/审核/过账 | ✅ 已实现 |
| P1 | Excel 批量导入工资 | ⚠️ 模型有 source='excel' 字段，UI 待补 |
| P1 | 个税自动计算 | ❌ 当前手动填 individual_tax |
| P1 | 银行代发文件生成 | ❌ 未实现 |
| P2 | 多工资项目配置化 | ❌ 字段已 hardcode |

### 15.3 预算管理

| 功能 | 说明 | 状态 |
|---|---|---|
| 预算项目管理 | 预算项增删改查 | ✅ 已实现 |
| 预算执行分析 | 实际 vs 预算对比 | ✅ 已实现 |

### 15.4 成本管理

| 功能 | 说明 | 状态 |
|---|---|---|
| 成本中心管理 | 成本中心增删改查 | ✅ 已实现 |
| 成本核算 | 按成本中心归集费用 | ✅ 已实现 |

### 15.5 档案管理

| 功能 | 说明 | 状态 |
|---|---|---|
| 档案列表 | 会计档案查询 | ✅ 已实现 |
| 借阅管理 | 档案借阅/归还流程 | ✅ 已实现 |

### 15.6 辅助核算

| 功能 | 说明 | 状态 |
|---|---|---|
| 客户管理 | 客户档案增删改查 | ✅ 已实现 |
| 供应商管理 | 供应商档案增删改查 | ✅ 已实现 |

### 15.7 系统设置

| 功能 | 说明 | 状态 |
|---|---|---|
| 用户管理 | 用户增删改查、角色分配 | ✅ 已实现 |
| 部门管理 | 部门树管理 | ✅ 已实现 |
| 角色权限 | 角色 CRUD、权限分配 | ✅ 已实现 |
| 公司信息 | 公司基本信息配置 | ✅ 已实现 |
| 科目映射 | 业务科目与会计科目映射 | ✅ 已实现 |
| 数据备份 | 数据库备份管理 | ✅ 已实现 |

### 15.8 审计日志

| 功能 | 说明 | 状态 |
|---|---|---|
| 日志查询 | 按用户、操作类型、时间范围查询 | ✅ 已实现 |

### 15.9 预警管理

| 功能 | 说明 | 状态 |
|---|---|---|
| 预警查询 | 系统预警信息查询 | ✅ 已实现 |

## 十六、销售到收款循环

### 16.1 应收单详细需求（核心债权载体，对应 V1.0 §3.2）

#### 16.1.1 字段定义

参考 Go 版模型 `internal/model/ar_invoice.go`：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| invoice_no | String(20) | 是 | 应收单号（唯一） |
| customer_id | UUID | 是 | 客户ID（债务人） |
| invoice_id | UUID | 是 | 来源销售发票ID |
| amount | Decimal | 是 | 应收金额（正数=债权，负数=红字冲销）|
| paid_amount | Decimal | 是 | 已核销金额（累计）|
| outstanding_amount | Decimal | 是 | 余额 = amount - paid_amount |
| due_date | Date? | 否 | 账期/到期日（赊销信用期截止）|
| status | String(20) | 是 | draft/confirmed/partially_paid/paid/reversed |
| source_type | String(20) | 是 | sales_invoice/manual/adjustment |
| voucher_id | UUID? | 否 | 关联凭证ID |
| locked_at | DateTime? | 否 | 业务锁时间（防并发核销）|
| locked_by | UUID? | 否 | 锁持有人 |

#### 16.1.2 状态流转

| 状态 | 触发条件 | 业务含义 |
|------|---------|---------|
| draft | 新建 | 草稿，未生效 |
| confirmed | 提交审核 | 债权确立 |
| partially_paid | 至少一次核销，余额 > 0 | 部分核销 |
| paid | outstanding_amount = 0 | 已核销，债权终结 |
| reversed | 红字冲销 | 债权被反向 |

#### 16.1.3 凭证生成规则

**蓝字应收单**（销售发票确认收入时）：
```
借：应收账款（amount）
  　　贷：主营业务收入
  　　贷：应交税费—应交增值税（销项税额）
```

**红字应收单**（退货/折让冲销）：
- 做蓝字凭证的负数红冲分录

#### 16.1.4 V1.0 报告要求 vs Go 版现状

| V1.0 要求 | Go 版实现 | 差异 |
|----------|---------|------|
| 应收单号 | ✅ invoice_no 字段 | 一致 |
| 客户（债务人）| ✅ customer_id | 一致 |
| 蓝/红标记 | ✅ amount 正负区分 | 一致 |
| 已核销金额 | ✅ paid_amount（命名差异）| 一致 |
| 余额 | ✅ outstanding_amount | 一致 |
| 账期/到期日 | ✅ due_date | 一致 |
| 状态：未核销/部分核销/已核销/已逾期 | ⚠️ 有 paid 系列状态，**无"已逾期"** | **P0 缺失** |
| 来源发票ID | ✅ invoice_id | 一致 |
| 币种、汇率 | ❌ 无 currency 字段 | **P0 缺失** |

### 16.2 收款单详细需求（对应 V1.0 §3.3）

#### 16.2.1 字段定义

参考 Go 版模型 `internal/model/payment.go`（通用模型，PaymentType 区分收/付）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| payment_no | String(20) | 是 | 收/付款单号 |
| payment_type | String(20) | 是 | receipt（收款）/ payment（付款）|
| party_type | String(20) | 是 | customer/supplier |
| party_id | UUID | 是 | 收/付款对象ID |
| counterparty_name | String? | 否 | 对方户名（手工录入场景）|
| paid_from_id | UUID? | 否 | 付款来源账户（银行存款）|
| paid_to_id | UUID? | 否 | 收款目标账户 |
| paid_amount | Decimal | 是 | 收/付金额 |
| received_amount | Decimal? | 否 | 实际到账金额（仅收款）|
| reference_no | String? | 否 | 银行流水号 |
| reference_date | Date? | 否 | 银行交易日期 |
| posting_date | Date | 是 | 入账日期 |
| bank_account_id | UUID? | 否 | 银行账户ID |
| docstatus | Int | 是 | 0=草稿/1=已提交/2=已审核/3=已过账/4=已作废 |
| voucher_id | UUID? | 否 | 关联凭证ID |
| description | String(500)? | 否 | 备注 |
| payment_method | String(20)? | 否 | bank/cash/wechat/alipay/transfer |

#### 16.2.2 与预收款的关系

Go 版采用**分离设计**：
- `payment_entries` 收/付款主表（业务流）
- `advance_receipts` 预收款表（资金流）
- 收款未指定应收单时，作为**预收款**记录到 `advance_receipts`

#### 16.2.3 凭证生成规则

**收款时**（收款单与应收单核销）：
```
借：银行存款
  　　贷：应收账款（按核销金额逐笔冲减）
```

**未核销前的暂挂**：
```
借：银行存款
  　　贷：预收账款
```

**付款时**（退款场景）：
```
借：应收账款（冲减预收款或清零红字冲后贷方）
  　　贷：银行存款
```

#### 16.2.4 V1.0 报告要求 vs Go 版现状

| V1.0 要求 | Go 版实现 | 差异 |
|----------|---------|------|
| 多种收款方式（银行/现金/第三方）| ✅ payment_method 字段 | 一致 |
| 流水号、交易时间 | ✅ reference_no / reference_date | 一致 |
| 客户、收款金额 | ✅ party_id / paid_amount | 一致 |
| 未核销金额独立字段 | ⚠️ 用 outstanding_amount（不是独立 unallocated 字段）| **P1 字段拆分** |
| 币种 | ❌ 无 currency 字段 | **P0 缺失** |
| 不指定应收单时作为预收款 | ✅ 拆 advance_receipts 模型 | 一致（更严谨）|

### 16.3 核销管理详细需求（核心勾稽引擎，对应 V1.0 §3.4）

#### 16.3.1 核销类型

| 类型 | 业务场景 | Go 版实现 |
|------|---------|----------|
| 收款与应收核销 | 一笔收款单冲销一笔或多笔应收单余额 | ✅ `/payment-entries/:id/allocate` |
| 应收对冲（红蓝对冲）| 蓝字应收单与红字应收单直接对冲 | ✅ `ReconciliationService` |
| 预收冲应收 | 客户预收款转销应收 | ✅ `advance_receipts` outstanding + allocate |
| 银企对账核销 | 银行流水与凭证/应收匹配 | ✅ `bank-reconciliation/*` 路由 |

#### 16.3.2 核销明细表

参考 Go 版 `payment_allocations` 表：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| payment_entry_id | UUID | 收/付款单ID |
| invoice_id | UUID | 被核销的应收/应付单ID |
| invoice_type | String? | ar_invoice / ap_invoice |
| allocated_amount | Decimal | 本次核销金额 |
| created_at | DateTime | 核销时间 |

#### 16.3.3 核销操作界面需求

按 V1.0 §3.4.2：
- 选择客户，系统自动展示该客户下所有余额不为零的应收单（正负均列）和未核销完毕的收款单
- 用户手动勾选本次需冲销的单据，并输入各单据本次冲销金额
- 系统进行金额平衡校验：∑借方冲销金额 = ∑贷方冲销金额
- 支持部分核销
- **自动按到期日顺序建议核销** — ⚠️ **Go 版可能未实现（待查）**

#### 16.3.4 原子事务保证

- 核销成功后，立即更新应收单的 `paid_amount` 和 `outstanding_amount`，并联动更新收款单
- 若应收单余额归零，状态变为 `paid`；否则保持 `partially_paid`
- **严禁直接修改余额字段而不记录核销明细**（业务规则）
- Go 版使用 `LockedAt`/`LockedBy` 业务锁防止并发核销（**非版本号乐观锁**）

#### 16.3.5 V1.0 报告要求 vs Go 版现状

| V1.0 要求 | Go 版实现 | 差异 |
|----------|---------|------|
| 收款与应收核销 | ✅ payment-entries/:id/allocate | 一致 |
| 应收对冲（红蓝）| ✅ ReconciliationService | 一致 |
| 预收冲应收 | ✅ advance_receipts | 一致 |
| 金额平衡校验 | ✅ 在 service 层 | 一致 |
| 部分核销 | ✅ partially_paid 状态 | 一致 |
| 自动按到期日建议核销 | ⚠️ 待查 | **P1 待查/实现** |
| 核销明细表 | ✅ payment_allocations | 一致 |
| 原子事务更新 | ✅ Go service 用事务 | 一致 |
| 乐观锁 | ⚠️ 用 LockedAt 业务锁（**非版本号**）| **P2 概念差异** |

### 16.4 预收款管理详细需求（对应 V1.0 §3.6）

#### 16.4.1 数据模型

Go 版独立模型 `advance_receipts`：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| advance_no | String | 是 | 预收款单号 |
| customer_id | UUID | 是 | 客户ID |
| amount | Decimal | 是 | 预收金额 |
| allocated_amount | Decimal | 是 | 已核销金额 |
| outstanding_amount | Decimal | 是 | 未核销金额 = amount - allocated_amount |
| received_date | Date | 是 | 收到预收款日期 |
| due_date | Date? | 否 | 预计转销日期 |
| status | String | 是 | draft/confirmed/partially_allocated/fully_allocated/reversed |
| source_type | String | 是 | bank/manual/adjustment |
| bank_account_id | UUID? | 否 | 收款银行账户 |
| reference_no | String? | 否 | 银行流水号 |
| voucher_id | UUID? | 否 | 关联凭证 |
| voucher_no | String? | 否 | 凭证号 |

#### 16.4.2 业务规则

- **预收冲应收**：通过 allocate 动作将预收款转销应收单
- **状态机**：draft → confirmed → partially_allocated → fully_allocated
- **凭证规则**：
  ```
  收到预收款：借：银行存款  贷：预收账款
  转销应收：  借：预收账款  贷：应收账款
  ```

#### 16.4.3 V1.0 报告要求 vs Go 版现状

| V1.0 要求 | Go 版实现 | 差异 |
|----------|---------|------|
| 收款未核销即预收 | ✅ 拆 advance_receipts 模型 | 一致（更严谨）|
| 预收报表（按客户查询）| ⚠️ ListOutstanding 提供了列表，**无预收汇总统计** | **P0 缺失** |
| 预收冲应收 | ✅ allocate 动作 | 一致 |

### 16.5 坏账计提与核销详细需求（对应 V1.0 §3.7）

> ⚠️ **P0 缺失** — Go 版尚未实现坏账计提/核销模块，需要下一轮开发。

#### 16.5.1 业务场景

1. **坏账计提**（定期，如月末）：对逾期的应收单按账龄比例计提坏账准备
2. **坏账核销**（确认损失）：当确定款项无法收回时，将坏账准备与应收账款冲销

#### 16.5.2 凭证规则

**坏账计提**（按账龄比例，如 1-30天 5%、31-60天 20%、60+天 50%）：
```
借：信用减值损失
  　　贷：坏账准备
```
（仅标记应收单为"已计提坏账"，不改变应收单余额）

**坏账核销**（确认损失）：
```
借：坏账准备
  　　贷：应收账款
```
（应收单余额冲减为 0，通过特殊核销处理）

#### 16.5.3 字段设计建议

需新增：

| 表 | 字段 | 类型 | 说明 |
|------|------|------|------|
| ar_invoices | bad_debt_reserved | Decimal | 已计提坏账金额 |
| ar_invoices | bad_debt_reserved_at | DateTime | 计提时间 |
| ar_invoices | bad_debt_written_off | Bool | 是否已核销为损失 |
| new table | bad_debt_provisions | — | 坏账计提单（按期间） |
| new table | bad_debt_writeoffs | — | 坏账核销单（按笔） |

#### 16.5.4 API 清单（待开发）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/bad-debt/provisions` | 月末计提坏账 |
| GET | `/api/v1/bad-debt/provisions` | 计提记录列表 |
| POST | `/api/v1/bad-debt/writeoffs` | 确认坏账核销 |
| GET | `/api/v1/bad-debt/writeoffs` | 核销记录列表 |
| GET | `/api/v1/ar-invoices/aging` | 账龄分析（基础数据） |

#### 16.5.5 开发优先级

| 优先级 | 功能 | 状态 |
|--------|------|------|
| P0 | 应收单增加"已逾期"状态字段 | ❌ 待开发 |
| P0 | 坏账计提单（按期间生成）| ❌ 待开发 |
| P0 | 坏账核销单（按笔生成）| ❌ 待开发 |
| P0 | 应收单 bad_debt_reserved 字段 | ❌ 待开发 |
| P1 | 账龄分析表增强（按账龄分组）| ⚠️ 已有 aging_handler 待对齐 |
| P1 | 预收汇总报表 | ❌ 待开发 |
| P1 | 多币种/汇率支持 | ❌ 待开发 |

## 十七、采购到付款循环

### 17.1 应付单详细需求（核心债务载体，对应采购 V1.0 §3.2）

#### 17.1.1 字段定义

参考 Go 版模型 `internal/model/ap_invoice.go`（与应收单 ArInvoice 高度对称）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| invoice_no | String(20) | 是 | 应付单号（唯一） |
| supplier_id | UUID | 是 | 供应商ID（债权人） |
| invoice_id | UUID | 是 | 来源采购发票ID |
| amount | Decimal | 是 | 应付金额（正数=债务，负数=红字冲销）|
| paid_amount | Decimal | 是 | 已核销金额（累计）|
| outstanding_amount | Decimal | 是 | 余额 = amount - paid_amount |
| due_date | Date? | 否 | 账期/到期日（应付款项的约定支付期限）|
| status | String(20) | 是 | draft/confirmed/partially_paid/paid/reversed |
| source_type | String(20) | 是 | expense_invoice/manual/adjustment |
| remark | String(500)? | 否 | 备注 |
| voucher_id | UUID? | 否 | 关联凭证ID |

**注意**：
- ❌ 当前 **无** `currency` / `exchange_rate` 字段（**V2 远期**）
- ❌ 当前 **无** `locked_at` / `locked_by` 字段（**TASK-3.2 P0 待补**）

#### 17.1.2 状态流转

| 状态 | 触发条件 | 业务含义 |
|------|---------|---------|
| draft | 新建 | 草稿，未生效 |
| confirmed | 提交审核 | 债务确立 |
| partially_paid | 至少一次核销，余额 > 0 | 部分核销 |
| paid | outstanding_amount = 0 | 已核销，债务终结 |
| reversed | 红字冲销 | 债务被反向 |
| **overdue** | due_date < 当前日期 且 outstanding_amount > 0 | **已逾期（TASK-3.3 P0 待补）**|

#### 17.1.3 凭证生成规则

**蓝字应付单（存货采购）**：
```
借：库存商品 / 原材料
借：应交税费—应交增值税（进项税额）
  　　贷：应付账款
```

**蓝字应付单（费用采购）**：
```
借：管理费用 / 销售费用
借：应交税费—进项税额
  　　贷：应付账款
```

**红字应付单**：做蓝字凭证的负数红冲分录

#### 17.1.4 V1.0 要求 vs Go 版现状

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 应付单号 | ✅ invoice_no | ✅ |
| 供应商（债权人）| ✅ supplier_id | ✅ |
| 蓝/红标记 | ✅ amount 正负 | ✅ |
| 已核销金额 | ✅ paid_amount | ✅ |
| 余额 | ✅ outstanding_amount | ✅ |
| 账期/到期日 | ✅ due_date | ✅ |
| 状态：未核销/部分核销/已核销 | ✅ draft/partially_paid/paid | ✅ |
| **状态：已逾期** | ❌ 无 | **P0 缺失** |
| 来源发票ID | ✅ invoice_id | ✅ |
| **币种、汇率** | ❌ 无 | **V2 远期** |
| **handler 端点** | ⚠️ 仅 List/GetByID | **P0 缺失** |

### 17.2 付款单详细需求（采购端视角，对应 V1.0 §3.3）

#### 17.2.1 与销售端的关系

Go 版付款单采用**通用模型** `payment_entries`，与销售端收款单共用：
- `payment_type` 字段：`receipt`（收款）/ `payment`（付款）
- 采购端付款 → `payment_type='payment'`，`party_type='supplier'`
- 销售端收款 → `payment_type='receipt'`，`party_type='customer'`

**详细字段定义参考 15.11 收款单详细需求**（已与采购端对称）。

#### 17.2.2 采购端业务规则

- **付款方式**：银行转账、现金、票据（银行承兑汇票、商业承兑汇票）
- **付款审批**：先提交（docstatus=1）→ 财务主管审核（docstatus=2）→ 过账（docstatus=3）
- **银行流水关联**：可选关联 bank_transactions（自动核销场景）
- **凭证生成**：
  - 直接核销应付：借：应付账款  贷：银行存款
  - 作为预付款：借：预付账款  贷：银行存款

#### 17.2.3 V1.0 要求 vs Go 版现状

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 多种付款方式（银行/现金/票据）| ✅ payment_method 字段 | ✅ |
| 流水号、账号、支票号 | ✅ reference_no 字段 | ✅ |
| 供应商、付款金额 | ✅ party_id/paid_amount | ✅ |
| 不关联应付单即预付 | ✅ advance_payments 模型 | ✅ |
| 凭证规则（直接核销/预付）| ✅ GenerateFromPaymentEntry | ✅ |
| **币种** | ❌ 无 | **V2 远期** |

### 17.3 采购退货流程详细需求（对应 V1.0 §3.5）

#### 17.3.1 退货场景矩阵

| 场景 | 流程 | 核销方式 | 资金处理 | 状态 |
|------|------|---------|---------|------|
| 全额退货，款未付 | 开红字采购发票→红字应付单 | 红蓝应付对冲 | 无退款，债务抵消 | ✅ 已实现 |
| 部分退货，款未付 | 开部分红字发票→部分红字应付 | 红蓝应付对冲 | 剩余债务继续按账期支付 | ✅ 已实现 |
| 已付款后发生退货 | 开红字采购发票→红字应付 | 对冲后形成公司应收供应商款项 | **生成收款单，向供应商索回退款，或转为预付款抵扣下次货款** | ✅ 已实现（通用 payment_entries）|
| 折让（不退货）| 同上红字发票流程 | 同上 | 同上 | ✅ 已实现 |

#### 17.3.2 供应商退款处理

当采购退货后供应商需退还已付款项时：

- 系统应支持生成**收款单**（对供应商的收入）
- 关联原付款单或合同
- 凭证：借：银行存款 贷：应付账款（或预付账款）

**Go 版实现**：payment_entries 通用模型 `payment_type='receipt'`，`party_type='supplier'` 即可表达"对供应商的收款"。

#### 17.3.3 V1.0 要求 vs Go 版现状

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 全额退货款未付→红字应付 | ✅ 部分红冲 | ✅ |
| 部分退货款未付 | ✅ is_part_red | ✅ |
| 已付款后退货→收款单 | ✅ 通用 payment_entries | ✅ |
| 供应商退款凭证 | ✅ 借：银行存款 贷：应付账款/预付账款 | ✅ |
| **采购订单/入库单关联** | ❌ 无 | **V2 远期** |

### 17.4 预付款管理详细需求（对应 V1.0 §3.6）

#### 17.4.1 数据模型

Go 版独立模型 `advance_payments`（与销售端 `advance_receipts` 对称）：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | UUID | 是 | 主键 |
| advance_no | String | 是 | 预付款单号 |
| supplier_id | UUID | 是 | 供应商ID |
| amount | Decimal | 是 | 预付金额 |
| allocated_amount | Decimal | 是 | 已核销金额 |
| outstanding_amount | Decimal | 是 | 未核销金额 = amount - allocated_amount |
| paid_date | Date | 是 | 预付款支付日期 |
| due_date | Date? | 否 | 预计转销日期 |
| status | String | 是 | draft/confirmed/partially_allocated/fully_allocated/reversed |
| source_type | String | 是 | bank/manual/adjustment |
| bank_account_id | UUID? | 否 | 付款银行账户 |
| reference_no | String? | 否 | 银行流水号 |
| voucher_id | UUID? | 否 | 关联凭证 |
| voucher_no | String? | 否 | 凭证号 |

#### 17.4.2 业务规则

- **预付冲应付**：通过 allocate 动作将预付款转销应付单
- **状态机**：draft → confirmed → partially_allocated → fully_allocated
- **凭证规则**：
  ```
  支付预付款：借：预付账款  贷：银行存款
  转销应付：  借：应付账款  贷：预付账款
  ```

#### 17.4.3 V1.0 要求 vs Go 版现状

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 付款未核销即预付 | ✅ advance_payments | ✅ |
| **预付款台账（按供应商）** | ⚠️ ListOutstanding 有列表，**无按供应商汇总的余额表** | **P0 缺失（TASK-3.5）** |
| 预付冲应付 | ✅ allocate | ✅ |
| **跨币种预付汇率差异** | ❌ 无 | **V2 远期** |

### 17.5 现金折扣详细需求（对应 V1.0 §3.7，采购端独有 P1）— **TASK-3.4 P0 已实现 ✅**

#### 17.5.1 业务场景

供应商为鼓励提前付款提供折扣（如 2/10 N/30）：
- 10 天内付款 → 2% 折扣
- 30 天内付款 → 全额
- 30 天后付款 → 可能产生违约金

**Go 版 V6.6 状态**：✅ **P0 已实现并通过端到端测试**（commit `99901fcd` + `95af7ee1`，2026-06-08 03:25 验证）

#### 17.5.2 业务规则

- 付款金额可能小于应付单余额
- 差额作为折扣
- 核销时录入"本次折扣金额"
- 凭证规则：
  ```
  借：应付账款（全额应付）
    　　贷：银行存款（实付）
    　　贷：财务费用—利息收入（折扣部分）
  ```

#### 17.5.3 字段设计建议

需新增：

| 表 | 字段 | 类型 | 说明 |
|------|------|------|------|
| new table | cash_discounts | — | 折扣记录主表 |
| payment_allocations | discount_amount | Decimal(18,2) | 本次折扣金额 |

**CashDiscount 模型**：
- id, payment_entry_id, ap_invoice_id
- discount_amount, discount_rate（如 0.02 = 2%）
- created_at, created_by

#### 17.5.4 API 清单（待开发）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/cash-discounts` | 折扣记录列表 |
| GET | `/api/v1/cash-discounts/:id` | 详情 |
| GET | `/api/v1/cash-discounts/by-payment/:payment_entry_id` | 按付款单查 |
| 改造 | `POST /payment-entries/:id/allocate` | 增加可选 `discount_amount` 参数 |

#### 17.5.5 V1.0 要求 vs Go 版现状（V6.7 更新）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 现金折扣模型 | ✅ `cash_discounts` 表 (migrations/058) | ✅ TASK-3.4-A |
| 核销录入折扣金额 | ✅ `payment_allocations.discount_amount` 字段 | ✅ TASK-3.4-B |
| 凭证：借：应付账款 贷：银行存款 贷：财务费用 | ✅ `GenerateCashDiscountVoucher` 3-line voucher | ✅ TASK-3.4-C |
| **payment_allocations 加 discount_amount** | ✅ 已添加并测试 | ✅ TASK-3.4-B |
| **discount_amount → 3-line 凭证端到端** | ✅ **HTTP 200 + DB 验证** | ✅ **TASK-3.4-D** |
| 折扣记录列表/查询 API | ❌ 无 | P1 待开发 |

#### 17.5.6 开发优先级

| 优先级 | 功能 | 状态 |
|--------|------|------|
| P0 | cash_discounts 表 | ✅ migrations/058 已建 |
| P0 | payment_allocations 加 discount_amount 字段 | ✅ TASK-3.4-B |
| P0 | 核销 API 支持 discount_amount 参数 | ✅ handler 已改 decimal |
| P0 | 折扣凭证自动生成 | ✅ TASK-3.4-C + D 验证 |
| P1 | 折扣记录列表/查询 API | ❌ 待开发 |
| P1 | 多折扣策略配置（如 2/10 N/30 自动判断）| ❌ 待开发 |

***

## 附录 A：Go 版 vs V1.0 报告 现状对比表（2026-06-08）

> 本附录给出 Go 版代码实际实现与用户 V1.0 报告的逐项差异，**仅作现状对照**。**P0/P1/P2 标记含义**：
> - P0 = 核心业务缺失，建议下一轮优先开发
> - P1 = 增强功能，中优先级
> - P2 = 概念差异，可后续讨论

### 附 A.1 应收单管理（V1.0 §3.2）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 应收单号（唯一标识）| ✅ invoice_no 字段 | ✅ |
| 客户（债务人）| ✅ customer_id | ✅ |
| 蓝/红标记 | ✅ amount 正负区分 | ✅ |
| 已核销金额 | ✅ paid_amount | ✅ |
| 余额 | ✅ outstanding_amount | ✅ |
| 账期/到期日 | ✅ due_date | ✅ |
| 状态：未核销/部分核销/已核销 | ✅ draft/partially_paid/paid | ✅ |
| **状态：已逾期** | ❌ **无此状态** | **P0 缺失** |
| 来源发票ID | ✅ invoice_id | ✅ |
| **币种、汇率** | ❌ **无 currency 字段** | **P0 缺失** |

### 附 A.2 收款单管理（V1.0 §3.3）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 多种收款方式 | ✅ payment_method | ✅ |
| 流水号、交易时间 | ✅ reference_no/reference_date | ✅ |
| 客户、收款金额 | ✅ party_id/paid_amount | ✅ |
| 未核销金额独立字段 | ⚠️ 用 outstanding_amount | P1 字段拆分 |
| 不指定应收单即预收款 | ✅ advance_receipts 模型 | ✅ |
| **币种** | ❌ **无** | **P0 缺失** |

### 附 A.3 核销管理（V1.0 §3.4）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 收款与应收核销 | ✅ payment-entries/:id/allocate | ✅ |
| 应收对冲（红蓝）| ✅ ReconciliationService | ✅ |
| 预收冲应收 | ✅ advance_receipts | ✅ |
| 金额平衡校验 | ✅ service 层 | ✅ |
| 部分核销 | ✅ partially_paid | ✅ |
| 自动按到期日建议 | ⚠️ 待查 | P1 待查 |
| 核销明细表 | ✅ payment_allocations | ✅ |
| 原子事务 | ✅ Go transaction | ✅ |
| 乐观锁 | ⚠️ LockedAt 业务锁 | P2 概念差异 |

### 附 A.4 预收款管理（V1.0 §3.6）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 收款未核销即预收 | ✅ advance_receipts | ✅ |
| **预收汇总报表** | ❌ **仅有 ListOutstanding 列表** | **P0 缺失** |
| 预收冲应收 | ✅ allocate | ✅ |

### 附 A.5 坏账计提与核销（V1.0 §3.7）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 坏账计提（按账龄比例）| ❌ **无** | **P0 缺失** |
| 坏账核销（确认损失）| ❌ **无** | **P0 缺失** |
| 应收单"已计提坏账"标记 | ❌ **无** | **P0 缺失** |
| 坏账准备科目 | ❌ **无** | **P0 缺失** |

### 附 A.6 查询与报表（V1.0 §3.8）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 应收账款明细账 | ✅ 应收单+核销记录可拼 | ✅ |
| 账龄分析表 | ✅ aging_handler.go | ✅ |
| **客户对账单** | ⚠️ report_handler 需查 | P1 待查 |
| 未核销发票追踪 | ✅ invoice→ar_invoice 关联 | ✅ |

### 附 A.7 退货流程（V1.0 §3.5）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 全额退货款未收→红字应收 | ✅ 部分红冲 | ✅ |
| 部分退货款未收 | ✅ is_part_red | ✅ |
| 已收款后退货→付款单退款 | ✅ PaymentEntry 通用 | ✅ |

### 附 A.8 字段命名差异（文档统一性）

| V1.0 术语 | Go 字段名 | 说明 |
|----------|----------|------|
| 已核销金额 | `paid_amount` | 概念相同，命名差异 |
| 未核销金额 | `outstanding_amount` | 概念相同，命名差异 |
| 状态：已逾期 | (无此枚举) | 缺失 |

### 附 A.9 P0 补全任务清单（建议下一轮开发）

| # | 任务 | 工作量 | 涉及文件 |
|---|------|--------|---------|
| 1 | 应收单"已逾期"状态字段 | 0.5 天 | model/ar_invoice.go + migration + service |
| 2 | 多币种/汇率支持 | 1-2 天 | ar_invoice/payment/advance_receipt 加 currency + exchange_rate 表 + service |
| 3 | 坏账计提（按账龄比例）| 1-2 天 | 新建 bad_debt_provisions 表 + service + handler + UI |
| 4 | 坏账核销（确认损失）| 1 天 | 新建 bad_debt_writeoffs 表 + service + handler + UI |
| 5 | 应收单 bad_debt_reserved 字段 | 0.5 天 | ar_invoice 加字段 + migration + 计提联动 |
| 6 | 预收汇总报表 | 0.5 天 | advance_receipt 加 summary handler |

**P0 合计工作量：5-7 天**

### 附 A.10 采购端 P0 任务清单（V6.5 附录 B 引用）

| # | 任务 | 工作量 | 涉及文件 | 任务书编号 |
|---|------|--------|---------|-----------|
| 1 | 应付单 handler 端点补全（Confirm/Approve/Allocate）| 0.5 天 | ap_invoice_handler.go + service | TASK-3.1 |
| 2 | 应付单并发控制字段（locked_at / locked_by）| 0.5 天 | ap_invoice.go + migration | TASK-3.2 |
| 3 | 应付单"已逾期"状态（与销售端同步开发）| 0.5 天 | ap_invoice.go + migration + cron | TASK-3.3 |
| 4 | 现金折扣模型 + service | 1-2 天 | 新建 cash_discount 文件 + payment_allocations 加字段 | TASK-3.4 |
| 5 | 预付款余额表（按供应商）| 0.5 天 | advance_payment_handler.go 加 summary | TASK-3.5 |
| 6 | 采购发票→应付单链路检查 | 0.5 天 | expense_invoice_service.go | TASK-3.6 |
| 7 | 供应商对账单 | 0.5 天 | report_handler.go | TASK-3.7 |
| 8 | 应付账龄分析 | 0.5 天 | aging_handler.go | TASK-3.8 |
| 9 | 进项发票"已认证"状态 | 0.5 天 | expense_invoice.go + migration | TASK-3.9 |
| 10 | 核销自动按到期日建议 | 1 天 | reconciliation_service.go | TASK-3.10 |

**采购端 P0 合计：5-7 天开发** + **销售端 P0 合计：5-7 天** = **总 P0 工作量 10-14 天**

---

## 附录 B：Go 版 vs 采购到付款 V1.0 报告 现状对比表（2026-06-08）

> 本附录给出 Go 版代码实际实现与用户采购到付款 V1.0 报告的逐项差异，**仅作现状对照**。P0/P1/V2 标记含义：
> - P0 = 核心业务缺失，建议下一轮优先开发
> - P1 = 增强功能，中优先级
> - V2 = 远期规划（采购订单/入库单/多币种等），暂不实现

### 附 B.1 采购发票管理（V1.0 §3.1）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 蓝字发票（供应商/明细/金额/税率）| ✅ `expense_invoices`（V6.3 14.3 已覆盖）| ✅ |
| **采购订单/入库单关联（暂估-冲回）** | ❌ **无** | **V2 远期** |
| 红字发票关联原蓝字 + 原因 + 数量 | ✅ `original_invoice_id` / `is_part_red` | ✅ |
| 税务红字信息表编号 | ✅ `red_letter_info_id` | ✅ |
| 红字金额校验 | ⚠️ 待查 | 待查 |

### 附 B.2 应付单管理（V1.0 §3.2）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 应付单号（唯一）| ✅ `invoice_no` | ✅ |
| 供应商（债权人）| ✅ `supplier_id` | ✅ |
| 蓝/红标记 | ✅ amount 正负 | ✅ |
| 已核销金额 | ✅ `paid_amount` | ✅ |
| 余额 | ✅ `outstanding_amount` | ✅ |
| 账期/到期日 | ✅ `due_date` | ✅ |
| 状态：未核销/部分核销/已核销 | ✅ draft/partially_paid/paid | ✅ |
| **状态：已逾期** | ❌ 无 | **P0 缺失（TASK-3.3）** |
| 来源发票ID | ✅ `invoice_id` | ✅ |
| **币种、汇率** | ❌ 无 | **V2 远期** |
| **handler 端点** | ⚠️ **仅 List/GetByID** | **P0 缺失（TASK-3.1）** |

### 附 B.3 付款单管理（V1.0 §3.3）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 多种付款方式（银行/现金/票据）| ✅ `payment_method` | ✅ |
| 流水号、账号、支票号 | ✅ `reference_no` | ✅ |
| 供应商、付款金额 | ✅ `party_id` / `paid_amount` | ✅ |
| 不关联应付单即预付 | ✅ `advance_payments` 模型 | ✅ |
| 凭证规则（直接核销/预付）| ✅ GenerateFromPaymentEntry | ✅ |
| **币种** | ❌ 无 | **V2 远期** |

### 附 B.4 核销管理（V1.0 §3.4）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 付款与应付核销 | ✅ `payment-entries/:id/allocate` | ✅ |
| 应付对冲（红蓝）| ✅ ReconciliationService | ✅ |
| 预付冲应付 | ✅ `advance_payments` allocate | ✅ |
| 金额平衡校验 | ✅ service 层 | ✅ |
| 部分核销 | ✅ partially_paid | ✅ |
| 自动按到期日建议 | ⚠️ 待查 | **P1 缺失（TASK-3.10）** |
| 核销明细表 | ✅ `payment_allocations` | ✅ |
| 原子事务 | ✅ Go transaction | ✅ |
| **乐观锁** | ⚠️ **应付端无 locked_at 字段**（应收端有）| **P0 缺失（TASK-3.2）** |

### 附 B.5 退货流程集成（V1.0 §3.5）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 全额退货款未付→红字应付 | ✅ 部分红冲 | ✅ |
| 部分退货款未付 | ✅ is_part_red | ✅ |
| 已付款后退货→收款单（向供应商索回）| ✅ 通用 payment_entries | ✅ |
| 供应商退款凭证 | ✅ 借：银行存款 贷：应付账款/预付账款 | ✅ |

### 附 B.6 预付款管理（V1.0 §3.6）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 付款未核销即预付 | ✅ `advance_payments` | ✅ |
| **预付款台账（按供应商）** | ⚠️ ListOutstanding 有，**无按供应商汇总余额表** | **P0 缺失（TASK-3.5）** |
| 预付冲应付 | ✅ allocate | ✅ |
| **跨币种预付汇率差异** | ❌ 无 | **V2 远期** |

### 附 B.7 现金折扣（V1.0 §3.7，**采购端独有**）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 现金折扣模型（如 2/10 N/30）| ❌ **无** | **P0 缺失（TASK-3.4）** |
| 核销时录入折扣金额 | ❌ 无 | **P0 缺失（TASK-3.4）** |
| 凭证：借：应付账款 贷：银行存款 贷：财务费用 | ❌ 无 | **P0 缺失（TASK-3.4）** |
| **payment_allocations 加 discount 字段** | ❌ 当前只有 allocated_amount | **P0 缺失（TASK-3.4）** |

### 附 B.8 查询与报表（V1.0 §3.8）

| V1.0 要求 | Go 版实现 | 状态 |
|----------|---------|------|
| 应付账款明细账 | ✅ ApInvoice 列表 | ✅ |
| 应付账龄分析 | ⚠️ 与应收端共用 aging_handler？需查 | **P1 待查（TASK-3.8）** |
| **供应商对账单** | ⚠️ report_handler 需查 | **P1 缺失（TASK-3.7）** |
| 未核销发票追踪 | ✅ expense_invoice → ap_invoice 关联 | ✅ |
| **预付款余额表（按供应商）** | ❌ 无 | **P0 缺失（TASK-3.5）** |

### 附 B.9 字段命名差异（销售端/采购端对称）

| V1.0 术语 | Go 字段名 | 销售端 | 采购端 |
|----------|----------|--------|--------|
| 已核销金额 | `paid_amount` | ✅ | ✅ |
| 未核销金额 | `outstanding_amount` | ✅ | ✅ |
| 状态：已逾期 | (无) | **P0 缺失** | **P0 缺失** |
| 蓝/红标记 | amount 正负 | ✅ | ✅ |
| 锁字段 | `locked_at` / `locked_by` | ✅ | ❌ **P0 缺失** |

### 附 B.10 V2 远期规划（**不实现**）

| # | 需求 | 启动条件 | 工作量 |
|---|------|---------|--------|
| 1 | 采购订单/入库单模型（暂估-冲回流程）| 制造业/批发零售客户 | 3-5 天 |
| 2 | 应付单 currency / exchange_rate | 外贸/跨境业务客户 | 1-2 天 |
| 3 | 跨币种预付汇率差异 | 与多币种同步 | 1-2 天 |
| 4 | 多币种汇兑损益处理 | 与多币种同步 | 1 天 |
| 5 | 完整进项发票认证（国税平台对接）| 客户提出抵扣需求 | 2-3 天 |

**V2 合计工作量：8-13 天**（按业务需求触发）

### 附 B.11 P0/P1 任务清单（详细见 docs/tasks/采购到付款循环开发任务书_2026-06-08.md）

| 优先级 | 任务数 | 工作量 |
|--------|--------|--------|
| P0（核心业务）| 6 项（TASK-3.1 ~ 3.6）| 4-5 天 |
| P1（增强）| 4 项（TASK-3.7 ~ 3.10）| 2-3 天 |
| V2（远期）| 5 项 | 8-13 天 |

---

**附录 B 总结**：采购到付款 V1.0 报告覆盖了 Go 版 **约 70%** 的需求。**核心缺口**是：
1. **应付单 handler 端点补全**（仅 List/GetByID，无法走完整流程）
2. **现金折扣模型**（采购端独有，V1.0 新增场景）
3. **应付端并发控制字段**（与销售端对称）
4. **应付单"已逾期"状态**（与销售端对称）
5. **预付款余额表**（业务管理报表）
6. **采购发票→应付单链路检查**（可能断路，待验证）


## 十八、非功能需求

| 类别      | 指标               | 目标    |
| :------ | :--------------- | :------- |
| **性能**  | 报表生成时间           | ≤3秒      |
| **性能**  | 凭证保存响应时间         | ≤500ms   |
| **性能**  | Excel 导入处理时间      | ≤10秒/100行 |
| **可用性** | 系统可用性            | ≥99%     |
| **容量**  | 并发凭证处理           | 1000张/日  |
| **安全**  | 传输加密             | TLS 1.2+ |
| **安全**  | 敏感字段存储加密         | AES-256  |
| **安全**  | JWT Token 有效期      | Access 30min / Refresh 7day |

***

## 十九、远期规划（AI 增强）

> **规划阶段：V2**，当前 V1 采用确定性规则映射，V2 引入 AI 能力提升科目匹配的准确性与灵活性。

### 17.1 AI 智能科目推荐

**目标：** 在用户录入凭证或业务单据生成凭证时，根据摘要/备注/业务描述自动推荐最匹配的会计科目，减少手工选择成本。

| 场景 | 当前做法（V1） | V2 增强 |
|:---|:---|:---|
| 手工凭证录入 | 用户手动选择科目 | 输入摘要后自动推荐科目候选列表 |
| 业务单据生成凭证 | 按确定性规则映射（费用类型/客户/供应商） | 结合摘要语义修正科目推荐，覆盖规则未覆盖的边界情况 |
| 银行流水生成凭证 | 按分类字段 switch-case 映射 | 根据银行摘要+对方户名自动推测科目 |
| 科目映射配置 | 管理员手动设置映射表 | 系统根据历史使用数据推荐默认映射 |

**核心能力：**
1. **摘要理解**：解析中文财务摘要（如"付xx公司货款"、"收客户回款"），提取业务实质
2. **科目排序**：按历史使用频率 + 业务相似度对推荐科目排序
3. **人工反馈学习**：记录用户对推荐结果的采纳/修改，持续优化推荐质量
4. **冷启动**：初始阶段基于规则兜底，积累足够数据后切换为 AI 推荐为主

### 17.2 AI 凭证草稿

**目标：** 用户通过自然语言描述即可自动生成完整凭证（多行借贷分录）。

```
输入示例："报销差旅费2000元，员工张三"
输出：借 6602.03 管理费用-差旅费 2000 / 贷 1002 银行存款 2000
```

### 17.3 实现策略

| 阶段 | 范围 | 说明 |
|:---|:---|:---|
| V1（当前） | 确定性规则 | 按预设映射表/分类规则生成科目，见 §1.7 模块一 |
| V2.1 | 摘要分词+规则增强 | 引入中文分词提取关键词，扩展规则匹配弹性 |
| V2.2 | AI 推荐辅助 | 在规则无法覆盖时，调用轻量模型推荐科目，人工确认后生效 |
| V2.3 | AI 全自动推荐 | 默认使用 AI 推荐，保留规则兜底和人工覆盖能力 |

### 附 A.11 采购端 P0 任务完成状态清单（V6.6 → V6.7 更新）

> 任务开发依据：《"采购到付款"循环需求分析报告 V1.0》§3 业务详细需求 + 用户优先级指定。
> 当前进度：TASK-3.1 ~ TASK-3.6 全部代码 commit，**仅 TASK-3.4 现金折扣通过端到端测试**。

| 任务 | 描述 | 状态 | Commit | 端到端测试 |
|------|------|------|--------|------------|
| **TASK-3.1** | 应付单 handler 端点补全（10 路由）| ✅ 代码完成 | `2b2170a6` | ❌ 未测 |
| **TASK-3.2** | 应付单并发控制（locked_at 字段）| ✅ 代码完成 | `980862f0` | ❌ 未测 |
| **TASK-3.3** | 应付单"已逾期"状态 | ✅ 代码完成 | `edd9e105` | ❌ 未测 |
| **TASK-3.4** | 现金折扣（model + API + 3-line 凭证）| ✅ **代码+测试完成** | `c97bf266` + `46328757` + `99901fcd` + `95af7ee1` | ✅ **HTTP 200 + DB 验证** |
| **TASK-3.5** | 预付款余额表（按供应商/客户汇总）| ✅ 代码完成 | `589146db` | ❌ 未测 |
| **TASK-3.6** | 采购发票→应付单链路 | ⚠️ 部分（路由+handler 在 working tree）| — | ❌ 未测 |
| TASK-3.7 | 预付冲应付完整业务流 | ❌ 未开始 | — | — |
| TASK-3.8 | 采购退货红字流程 | ❌ 未开始 | — | — |
| TASK-3.9 | 应付对账单 | ❌ 未开始 | — | — |
| TASK-3.10 | 付款单分录生成 | ❌ 未开始 | — | — |

**完成度统计**：
- 代码完成：5/10（TASK-3.1, 3.2, 3.3, 3.4, 3.5）
- 代码+测试完成：1/10（TASK-3.4 现金折扣）✅
- 未开始：4/10（TASK-3.7 ~ 3.10）

**端到端验证详情（TASK-3.4）**：
```
POST /api/v1/payment-entries/.../allocate
body: { allocations: [{ invoice_id, allocated_amount: "1030.00", discount_amount: "100.00" }] }
→ HTTP 200
DB 副产物:
  - payment_allocations: 1 条 (alloc_id=3a3fb9a2-..., discount=100)
  - cash_discounts: 1 条 (rate=0.088496)
  - cash_discount_vouchers: 1 条 (invoice_type=sale)
  - journal_entries: 1 张 (voucher_no="CD-20260608-7036")
  - journal_entry_lines: 3 行（借 1122/1130, 贷 1002/1030, 贷 5602/100）
  - 借贷平衡: 1130 = 1030 + 100 ✅
```

**剩余 P0 验证任务**（下一步可执行）：
1. TASK-3.1：跑 10 个 ap_invoices API 端点
2. TASK-3.2：模拟两个并发 allocate，验证 locked_at 防重
3. TASK-3.3：创建 due_date 过期的 ap_invoice，验证 overdue 状态
4. TASK-3.5：创建 advance_payment + 冲抵，验证余额表更新
5. TASK-3.6：完成 supplier_id 字段 + Confirm 链路 + 验证 ApInvoice 自动创建
