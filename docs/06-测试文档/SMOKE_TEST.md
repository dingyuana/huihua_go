# 上线前冒烟测试清单 (Smoke Test Checklist)

版本：V1.0
适用：每次部署后必须执行，5 分钟可完成核心流程验证

---

## 一、服务存活

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 1.1 | `GET /health` 返回 | `{"status":"ok"}` | | ☐ |
| 1.2 | 通过 Nginx `GET /api/v1/health` | `{"status":"ok"}` | | ☐ |
| 1.3 | 前端 `GET /` 返回 HTML | 200 OK | | ☐ |
| 1.4 | 数据库连接 | `psql -c '\l'` 正常 | | ☐ |

## 二、登录与权限

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 2.1 | 用 `testuser/test123` 登录 | 跳转 `/dashboard` | | ☐ |
| 2.2 | 顶栏显示用户名 + 角色 | 显示 "testuser" + "admin" | | ☐ |
| 2.3 | 退出登录跳回 `/login` | 立刻跳转（≤ 1s） | | ☐ |
| 2.4 | 未登录访问受保护路由 | 重定向到 `/login` | | ☐ |

## 三、账套基础数据

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 3.1 | 科目表 `/setup/accounts` 至少 50 条 | 显示完整树形 | | ☐ |
| 3.2 | 资金账户 `/setup/bank-accounts` 至少 1 条 | 显示账户 | | ☐ |
| 3.3 | 至少 1 个现金账户（is_cash=true） | 标记"库存现金" | | ☐ |
| 3.4 | 分类规则 `/setup/rules` 至少 7 条 | 含 bank_fee/interest/tax/social_security | | ☐ |
| 3.5 | 凭证模板 `/setup/voucher-templates` | 可加载 | | ☐ |

## 四、流水导入 (P2)

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 4.1 | 上传 `7月对账单.xlsx` | 进度条显示 | | ☐ |
| 4.2 | 导入完成后总数=数据行数 | 26 条全部入库 | | ☐ |
| 4.3 | 失败明细展示 | 若有失败，显示失败行号+原因 | | ☐ |
| 4.4 | 银行手续费分类为 `bank_fee` | 标签"银行费用" | | ☐ |
| 4.5 | 利息/来账分类为 `interest_income` | 标签"利息收入" | | ☐ |

## 五、核对工作台 (P2/P3)

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 5.1 | `/bank/workbench` 加载 | 显示交易列表 | | ☐ |
| 5.2 | 选中"银行手续费"行 → 点击确认 | 调用 voucher 接口，生成凭证 | | ☐ |
| 5.3 | 后端日志可见 `POST /bank-transactions/:id/generate-voucher` | 200 OK | | ☐ |
| 5.4 | 数据库 voucher 数量 +1 | `SELECT count(*) FROM journal_entries` | | ☐ |
| 5.5 | 选中"业务收款"行 → 点击确认 | 调 payment-entries，生成收款单 | | ☐ |
| 5.6 | 收款单 payment_no 格式 | `REC-000001` | | ☐ |
| 5.7 | 批量确认：选 5 条不同分类 | 混合生成凭证+单据 | | ☐ |

## 六、期初余额 + 调整 (P1)

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 6.1 | 资金账户编辑支持期初余额 | 字段显示 | | ☐ |
| 6.2 | 调整余额 `POST /bank-accounts/:id/adjust-balance` | 201 + adjustment 记录 | | ☐ |
| 6.3 | `GET /bank-accounts/:id/balance-adjustments` | 返回审计列表 | | ☐ |
| 6.4 | 调整后 `current_balance` 已更新 | DB 验证 | | ☐ |

## 七、银企对账 (P4)

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 7.1 | `/bank-reconciliation/match` 页面加载 | 正常 | | ☐ |
| 7.2 | `/bank-reconciliation/balance` 余额调节表 | 展示余额概要 | | ☐ |
| 7.3 | `/bank-reconciliation/diff-report?bank_account_id=...&period_no=202407` | 返回 JSON | | ☐ |
| 7.4 | `/bank-reconciliation/balance-check` | 返回各账户余额一致性 | | ☐ |
| 7.5 | 当 current_balance ≠ 预期值 | 标记 `has_inconsistency: true` | | ☐ |

## 八、发票核销 (P3)

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 8.1 | `/invoices` 页面加载 | 列表显示 | | ☐ |
| 8.2 | `/reconciliation/manual` 加载未结清发票 | 真实数据（非 mock） | | ☐ |
| 8.3 | 选择发票 + 金额 → 执行核销 | `POST /reconciliation/manual` 201 | | ☐ |
| 8.4 | 核销后发票 outstanding 减少 | DB 验证 | | ☐ |

## 九、凭证 (P3)

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 9.1 | `/vouchers` 凭证列表 | 显示已生成的凭证 | | ☐ |
| 9.2 | 新增凭证 `/vouchers/create` | 表单可填写 | | ☐ |
| 9.3 | 三级科目级联选择 | 至少 1 级必选，2/3 级可空 | | ☐ |
| 9.4 | 借贷平衡校验 | 不平衡禁止提交 | | ☐ |
| 9.5 | 凭证审核工作台 `/vouchers/review` | 列表显示 | | ☐ |

## 十、报表

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 10.1 | `/period/reports` 利润表 | 渲染数据 | | ☐ |
| 10.2 | `/period/reports` 资产负债表 | 借贷合计平衡 | | ☐ |
| 10.3 | `/period/health-check` 结账体检 | 输出建议 | | ☐ |

## 十一、稳定性

| # | 检查项 | 期望 | 实测 | 通过 |
|---|--------|------|------|------|
| 11.1 | 连续 5 次刷新 `/dashboard` | 不出现 5xx | | ☐ |
| 11.2 | 后端日志无 ERROR 级别（除已知） | 干净 | | ☐ |
| 11.3 | 数据库无死锁 | `SELECT * FROM pg_locks` 正常 | | ☐ |
| 11.4 | 磁盘空间 > 30% | `df -h` | | ☐ |

---

## 通过标准

- 全部 ☐ 都打勾
- 如有失败项，必须在 `<production-deployment>` 前修复并复测

## 执行人 / 时间

| 字段 | 填写 |
|------|------|
| 执行人 |  |
| 时间 |  |
| 部署版本 | commit:  |
| 数据库版本 | migration: 028_xxx applied |
