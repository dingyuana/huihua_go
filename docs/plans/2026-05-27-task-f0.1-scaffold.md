# TASK-F0.1 实现计划 — Go + PostgreSQL RLS 项目脚手架

> **For implementer:** Use TDD throughout. Write failing test first. Watch it fail. Then implement.
> **Project:** AI财务SaaS（银行流水驱动业财一体化）
> **Task:** TASK-F0.1 技术选型与项目脚手架
> **Date:** 2026-05-27
> **Based on:** gstack /plan-eng-review 架构评审通过

---

## 目标（Goal）

搭建银行流水驱动业财一体化平台的 Go + PostgreSQL 项目骨架，实现：
1. JWT 认证 + tenant_id 注入
2. PostgreSQL RLS 多租户隔离（SET app.current_tenant）
3. Docker Compose 一键启动
4. `/health` 接口返回 `{"status":"ok"}`
5. RLS 跨租户隔离验证通过

---

## 技术栈（Tech Stack）

- Go 1.22+（Golang）
- GORM GEN（代码生成）
- pgx / pgxpool（PostgreSQL 驱动）
- Fiber（HTTP 框架，高性能）
- Viper（配置管理）
- Redis 7（缓存）
- Docker Compose（部署）
- PostgreSQL 15（RLS 多租户）

---

## 项目结构

```
huihua-finance/
├── cmd/
│   └── api/
│       └── main.go
├── internal/
│   ├── config/
│   │   └── config.go
│   ├── middleware/
│   │   ├── tenant.go
│   │   ├── auth.go
│   │   └── log.go
│   ├── handler/
│   │   └── health.go
│   ├── service/
│   ├── repository/
│   └── model/
├── pkg/
│   ├── database/
│   │   └── postgres.go
│   ├── redis/
│   │   └── client.go
│   └── jwt/
│       └── utils.go
├── migrations/
│   └── 001_init.sql
├── docker-compose.yml
├── Dockerfile
├── .env.example
└── go.mod
```

---

## 实现步骤

### Step 1: 初始化项目骨架 + go.mod

**文件：**
- 创建：`/root/data/disk/huihua-finance/go.mod`
- 创建：`/root/data/disk/huihua-finance/cmd/api/main.go`

**Step 1.1: 创建 go.mod**

```bash
mkdir -p /root/data/disk/huihua-finance/cmd/api
cd /root/data/disk/huihua-finance && go mod init huihua/finance
```

**go.mod 内容：**

```go
module huihua/finance

go 1.22

require (
    github.com/gofiber/fiber/v2 v2.52.0
    github.com/jackc/pgx/v5 v5.5.0
    github.com/redis/go-redis/v9 v9.4.0
    github.com/golang-jwt/jwt/v5 v5.2.0
    github.com/spf13/viper v1.18.0
    github.com/google/uuid v1.6.0
)
```

**Step 1.2: 创建 main.go**

```go
package main

import (
    "log"
    "github.com/gofiber/fiber/v2"
    "github.com/gofiber/fiber/v2/middleware/logger"
    "github.com/gofiber/fiber/v2/middleware/recover"
    "huihua/finance/internal/config"
    "huihua/finance/internal/handler"
    "huihua/finance/internal/middleware"
    "huihua/finance/pkg/database"
)

func main() {
    // Load config
    cfg := config.Load()
    
    // Init DB
    db, err := database.NewPostgres(cfg)
    if err != nil {
        log.Fatalf("failed to connect db: %v", err)
    }
    
    // Init Redis
    rdb := database.NewRedis(cfg)
    
    // Create Fiber app
    app := fiber.New(fiber.Config{
        ErrorHandler: func(c *fiber.Ctx, err error) error {
            return c.Status(fiber.StatusInternalServerError).JSON(fiber.Map{
                "error": err.Error(),
            })
        },
    })
    
    // Global middleware
    app.Use(logger.New())
    app.Use(recover.New())
    
    // Setup routes
    setupRoutes(app, db, rdb, cfg)
    
    log.Fatal(app.Listen(":8080"))
}

func setupRoutes(app *fiber.App, db *database.DB, rdb *database.RedisClient, cfg *config.Config) {
    // Health check (public)
    app.Get("/health", handler.HealthCheck)
    
    // Protected routes
    api := app.Group("/api/v1", middleware.Auth(cfg))
    api.Use(middleware.Tenant(db))
    
    // TODO: Add business handlers here
}
```

**Step 1.3: 验证**

```bash
cd /root/data/disk/huihua-finance && go mod tidy
# 预期：无错误，所有依赖解析成功
```

---

### Step 2: 配置管理（Viper）

**文件：**
- 创建：`/root/data/disk/huihua-finance/internal/config/config.go`
- 创建：`/root/data/disk/huihua-finance/.env.example`

**Step 2.1: config.go**

```go
package config

import (
    "github.com/spf13/viper"
    "os"
)

type Config struct {
    Server   ServerConfig
    Database DatabaseConfig
    Redis    RedisConfig
    JWT      JWTConfig
}

type ServerConfig struct {
    Host string
    Port string
}

type DatabaseConfig struct {
    Host     string
    Port     string
    User     string
    Password string
    DBName   string
    SSLMode  string
}

type RedisConfig struct {
    Host     string
    Port     string
    Password string
    DB      int
}

type JWTConfig struct {
    Secret string
    Expiry string // e.g. "30m", "7d"
}

func Load() *Config {
    // Support ENV override
    viper.SetEnvPrefix("HF")
    viper.AutomaticEnv()
    
    viper.SetDefault("server.host", "0.0.0.0")
    viper.SetDefault("server.port", "8080")
    viper.SetDefault("database.sslmode", "disable")
    viper.SetDefault("jwt.expiry", "30m")
    
    // Read .env file if exists
    viper.SetConfigName(".env")
    viper.SetConfigType("env")
    viper.AddConfigPath(".")
    _ = viper.ReadInConfig() // ignore error if no .env
    
    var cfg Config
    if err := viper.Unmarshal(&cfg); err != nil {
        panic(err)
    }
    return &cfg
}
```

**Step 2.2: .env.example**

```env
# Server
HF_SERVER_HOST=0.0.0.0
HF_SERVER_PORT=8080

# Database
HF_DATABASE_HOST=localhost
HF_DATABASE_PORT=5432
HF_DATABASE_USER=huihua
HF_DATABASE_PASSWORD=secret
HF_DATABASE_DBNAME=huihua_finance
HF_DATABASE_SSLMODE=disable

# Redis
HF_REDIS_HOST=localhost
HF_REDIS_PORT=6379
HF_REDIS_PASSWORD=
HF_REDIS_DB=0

# JWT
HF_JWT_SECRET=your-256-bit-secret-key-here
HF_JWT_EXPIRY=30m
```

**Step 2.3: 验证**

```bash
cd /root/data/disk/huihua-finance && go build ./internal/config/
# 预期：编译成功，无错误
```

---

### Step 3: JWT 工具 + Auth 中间件

**文件：**
- 创建：`/root/data/disk/huihua-finance/pkg/jwt/utils.go`
- 创建：`/root/data/disk/huihua-finance/internal/middleware/auth.go`

**Step 3.1: pkg/jwt/utils.go**

```go
package jwt

import (
    "errors"
    "time"
    "github.com/golang-jwt/jwt/v5"
    "github.com/google/uuid"
)

type Claims struct {
    UserID   uuid.UUID `json:"user_id"`
    TenantID uuid.UUID `json:"tenant_id"`
    Role     string    `json:"role"`
    jwt.RegisteredClaims
}

func GenerateToken(secret string, userID, tenantID uuid.UUID, role string, expiry time.Duration) (string, error) {
    claims := Claims{
        UserID:   userID,
        TenantID: tenantID,
        Role:     role,
        RegisteredClaims: jwt.RegisteredClaims{
            ExpiresAt: jwt.NewNumericDate(time.Now().Add(expiry)),
            IssuedAt:  jwt.NewNumericDate(time.Now()),
            NotBefore: jwt.NewNumericDate(time.Now()),
            Issuer:    "huihua-finance",
        },
    }
    token := jwt.NewWithClaims(jwt.SigningMethodHS256, claims)
    return token.SignedString([]byte(secret))
}

func ParseToken(secret, tokenStr string) (*Claims, error) {
    token, err := jwt.ParseWithClaims(tokenStr, &Claims{}, func(token *jwt.Token) (interface{}, error) {
        if _, ok := token.Method.(*jwt.SigningMethodHMAC); !ok {
            return nil, errors.New("unexpected signing method")
        }
        return []byte(secret), nil
    })
    if err != nil {
        return nil, err
    }
    if claims, ok := token.Claims.(*Claims); ok && token.Valid {
        return claims, nil
    }
    return nil, errors.New("invalid token")
}
```

**Step 3.2: internal/middleware/auth.go**

```go
package middleware

import (
    "github.com/gofiber/fiber/v2"
    "huihua/finance/internal/config"
    "huihua/finance/pkg/jwt"
)

func Auth(cfg *config.Config) fiber.Handler {
    return func(c *fiber.Ctx) error {
        authHeader := c.Get("Authorization")
        if authHeader == "" {
            return c.Status(fiber.StatusUnauthorized).JSON(fiber.Map{
                "error": "missing authorization header",
            })
        }
        
        // Extract Bearer token
        if len(authHeader) < 7 || authHeader[:7] != "Bearer " {
            return c.Status(fiber.StatusUnauthorized).JSON(fiber.Map{
                "error": "invalid authorization format",
            })
        }
        tokenStr := authHeader[7:]
        
        // Parse and validate
        claims, err := jwt.ParseToken(cfg.JWT.Secret, tokenStr)
        if err != nil {
            return c.Status(fiber.StatusUnauthorized).JSON(fiber.Map{
                "error": "invalid or expired token",
            })
        }
        
        // Store in locals
        c.Locals("user_id", claims.UserID)
        c.Locals("tenant_id", claims.TenantID)
        c.Locals("role", claims.Role)
        
        return c.Next()
    }
}

// Helper to get current user info from context
func GetUserID(c *fiber.Ctx) uuid.UUID {
    return c.Locals("user_id").(uuid.UUID)
}

func GetTenantID(c *fiber.Ctx) uuid.UUID {
    return c.Locals("tenant_id").(uuid.UUID)
}
```

---

### Step 4: PostgreSQL + RLS 中间件

**文件：**
- 创建：`/root/data/disk/huihua-finance/pkg/database/postgres.go`
- 创建：`/root/data/disk/huihua-finance/internal/middleware/tenant.go`
- 创建：`/root/data/disk/huihua-finance/migrations/001_init.sql`

**Step 4.1: pkg/database/postgres.go**

```go
package database

import (
    "context"
    "fmt"
    "github.com/jackc/pgx/v5/pgxpool"
    "github.com/google/uuid"
    "huihua/finance/internal/config"
)

type DB struct {
    pool *pgxpool.Pool
}

func NewPostgres(cfg *config.Config) (*DB, error) {
    dsn := fmt.Sprintf(
        "postgres://%s:%s@%s:%s/%s?sslmode=%s",
        cfg.Database.User,
        cfg.Database.Password,
        cfg.Database.Host,
        cfg.Database.Port,
        cfg.Database.DBName,
        cfg.Database.SSLMode,
    )
    poolConfig, err := pgxpool.ParseConfig(dsn)
    if err != nil {
        return nil, fmt.Errorf("parse dsn: %w", err)
    }
    
    // Connection pool: 10-25 for typical workload
    poolConfig.MaxConns = 20
    poolConfig.MinConns = 5
    
    pool, err := pgxpool.NewWithConfig(context.Background(), poolConfig)
    if err != nil {
        return nil, fmt.Errorf("create pool: %w", err)
    }
    
    // Verify connection
    if err := pool.Ping(context.Background()); err != nil {
        return nil, fmt.Errorf("ping db: %w", err)
    }
    
    return &DB{pool: pool}, nil
}

// SetTenant sets the RLS tenant context on the connection
// CRITICAL: Must be called before any business query
func (db *DB) SetTenant(tenantID uuid.UUID) error {
    _, err := db.pool.Exec(context.Background(), 
        fmt.Sprintf("SET app.current_tenant = '%s'", tenantID))
    return err
}

// GetPool returns the underlying pool
func (db *DB) GetPool() *pgxpool.Pool {
    return db.pool
}

// Close closes the connection pool
func (db *DB) Close() {
    db.pool.Close()
}
```

**Step 4.2: internal/middleware/tenant.go**

```go
package middleware

import (
    "github.com/gofiber/fiber/v2"
    "github.com/google/uuid"
    "huihua/finance/pkg/database"
)

// Tenant middleware: sets RLS tenant context per request
// Must run AFTER Auth middleware (so we have tenant_id from JWT)
func Tenant(db *database.DB) fiber.Handler {
    return func(c *fiber.Ctx) error {
        tenantID := c.Locals("tenant_id").(uuid.UUID)
        
        // Set tenant on this connection
        if err := db.SetTenant(tenantID); err != nil {
            return c.Status(fiber.StatusInternalServerError).JSON(fiber.Map{
                "error": "tenant switch failed",
            })
        }
        
        return c.Next()
    }
}

// TenantFromContext extracts tenant_id from Fiber locals
func TenantFromContext(c *fiber.Ctx) uuid.UUID {
    return c.Locals("tenant_id").(uuid.UUID)
}
```

**Step 4.3: migrations/001_init.sql**

```sql
-- 001_init.sql: Initial schema with RLS policies
-- All business tables include tenant_id for RLS isolation

-- Tenants table
CREATE TABLE IF NOT EXISTS tenants (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    domain VARCHAR(255),
    plan VARCHAR(50) DEFAULT 'basic',
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Users table
CREATE TABLE IF NOT EXISTS users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
    username VARCHAR(100) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    role VARCHAR(50) NOT NULL DEFAULT 'member',
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    UNIQUE(tenant_id, username)
);

-- Accounts table (chart of accounts, tree structure)
CREATE TABLE IF NOT EXISTS accounts (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
    parent_id UUID REFERENCES accounts(id) ON DELETE SET NULL,
    code VARCHAR(50) NOT NULL,
    name VARCHAR(255) NOT NULL,
    account_type VARCHAR(50) NOT NULL, -- asset/liability/equity/revenue/expense
    is_group BOOLEAN DEFAULT FALSE,
    is_active BOOLEAN DEFAULT TRUE,
    lft INT,  -- nested set left
    rgt INT,  -- nested set right
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    UNIQUE(tenant_id, code)
);

-- Enable RLS on all tables
ALTER TABLE tenants ENABLE ROW LEVEL SECURITY;
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
ALTER TABLE accounts ENABLE ROW LEVEL SECURITY;

-- RLS Policies: tenant isolation
CREATE POLICY tenant_isolation ON tenants
    USING (id = current_setting('app.current_tenant')::uuid);

CREATE POLICY tenant_isolation ON users
    USING (tenant_id = current_setting('app.current_tenant')::uuid);

CREATE POLICY tenant_isolation ON accounts
    USING (tenant_id = current_setting('app.current_tenant')::uuid);

-- For system admin: bypass RLS (use carefully)
-- CREATE POLICY tenant_bypass ON accounts
--     USING (current_setting('app.current_tenant') = '00000000-0000-0000-0000-000000000000');

-- Indexes for performance
CREATE INDEX idx_users_tenant ON users(tenant_id);
CREATE INDEX idx_accounts_tenant ON accounts(tenant_id);
CREATE INDEX idx_accounts_lft_rgt ON accounts(lft, rgt);
CREATE INDEX idx_accounts_parent ON accounts(parent_id);
```

---

### Step 5: Redis 客户端

**文件：**
- 创建：`/root/data/disk/huihua-finance/pkg/database/redis.go`

**Step 5.1: pkg/database/redis.go**

```go
package database

import (
    "context"
    "github.com/redis/go-redis/v9"
    "huihua/finance/internal/config"
)

type RedisClient struct {
    client *redis.Client
}

func NewRedis(cfg *config.Config) *RedisClient {
    client := redis.NewClient(&redis.Options{
        Addr:     cfg.Redis.Addr(),
        Password: cfg.Redis.Password,
        DB:       cfg.Redis.DB,
    })
    return &RedisClient{client: client}
}

func (r *RedisClient) Get(ctx context.Context, key string) (string, error) {
    return r.client.Get(ctx, key).Result()
}

func (r *RedisClient) Set(ctx context.Context, key string, value interface{}, ttl time.Duration) error {
    return r.client.Set(ctx, key, value, ttl).Err()
}

func (r *RedisClient) Del(ctx context.Context, key string) error {
    return r.client.Del(ctx, key).Err()
}

func (r *RedisClient) Client() *redis.Client {
    return r.client
}
```

---

### Step 6: Health Check Handler

**文件：**
- 创建：`/root/data/disk/huihua-finance/internal/handler/health.go`

**Step 6.1: internal/handler/health.go**

```go
package handler

import (
    "github.com/gofiber/fiber/v2"
)

func HealthCheck(c *fiber.Ctx) error {
    return c.JSON(fiber.Map{
        "status": "ok",
    })
}
```

**Step 6.2: 验证**

```bash
cd /root/data/disk/huihua-finance && go build ./cmd/api/
# 预期：编译成功，无错误
```

---

### Step 7: Docker 部署

**文件：**
- 创建：`/root/data/disk/huihua-finance/Dockerfile`
- 创建：`/root/data/disk/huihua-finance/docker-compose.yml`

**Step 7.1: Dockerfile**

```dockerfile
# Build stage
FROM golang:1.22-alpine AS builder

WORKDIR /app

# Install dependencies
RUN apk add --no-cache git

COPY go.mod go.sum ./
RUN go mod download

COPY . .

# Build
RUN CGO_ENABLED=0 GOOS=linux go build -o /api ./cmd/api

# Runtime stage
FROM alpine:3.19

RUN apk add --no-cache ca-certificates tzdata

WORKDIR /app

COPY --from=builder /api .
COPY .env.example .

EXPOSE 8080

CMD ["./api"]
```

**Step 7.2: docker-compose.yml**

```yaml
version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - HF_DATABASE_HOST=postgres
      - HF_DATABASE_PORT=5432
      - HF_DATABASE_USER=huihua
      - HF_DATABASE_PASSWORD=hfpwd
      - HF_DATABASE_DBNAME=huihua_finance
      - HF_DATABASE_SSLMODE=disable
      - HF_REDIS_HOST=redis
      - HF_REDIS_PORT=6379
      - HF_JWT_SECRET=dev-secret-change-in-prod
      - HF_JWT_EXPIRY=30m
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    command: ./api

  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: huihua_finance
      POSTGRES_USER: huihua
      POSTGRES_PASSWORD: hfpwd
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U huihua"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
```

---

### Step 8: 单元测试 + RLS 隔离验证

**文件：**
- 创建：`/root/data/disk/huihua-finance/pkg/jwt/utils_test.go`
- 创建：`/root/data/disk/huihua-finance/internal/middleware/auth_test.go`

**Step 8.1: pkg/jwt/utils_test.go**

```go
package jwt

import (
    "testing"
    "time"
    "github.com/google/uuid"
)

func TestGenerateAndParseToken(t *testing.T) {
    secret := "test-secret-key-256-bits-long!!"
    userID := uuid.New()
    tenantID := uuid.New()
    role := "admin"
    
    token, err := GenerateToken(secret, userID, tenantID, role, 30*time.Minute)
    if err != nil {
        t.Fatalf("generate token failed: %v", err)
    }
    
    if token == "" {
        t.Fatal("token should not be empty")
    }
    
    // Parse
    claims, err := ParseToken(secret, token)
    if err != nil {
        t.Fatalf("parse token failed: %v", err)
    }
    
    if claims.UserID != userID {
        t.Errorf("user_id mismatch: got %v, want %v", claims.UserID, userID)
    }
    if claims.TenantID != tenantID {
        t.Errorf("tenant_id mismatch: got %v, want %v", claims.TenantID, tenantID)
    }
    if claims.Role != role {
        t.Errorf("role mismatch: got %v, want %v", claims.Role, role)
    }
}

func TestParseToken_WrongSecret(t *testing.T) {
    secret := "test-secret"
    token, _ := GenerateToken(secret, uuid.New(), uuid.New(), "admin", 30*time.Minute)
    
    _, err := ParseToken("wrong-secret", token)
    if err == nil {
        t.Fatal("expected error for wrong secret")
    }
}
```

**Step 8.2: 验证**

```bash
cd /root/data/disk/huihua-finance && go test ./pkg/jwt/... -v
# 预期：PASS — jwt utils tests
```

**Step 8.3: RLS 集成测试计划**

```go
// internal/middleware/tenant_test.go
// 注意：需要 Docker Compose 运行 postgres

func TestTenantIsolation(t *testing.T) {
    if os.Getenv("INTEGRATION_TEST") != "1" {
        t.Skip("skipping integration test")
    }
    
    // 1. 创建两个租户 + 用户
    tenantA := createTenant("Tenant A")
    tenantB := createTenant("Tenant B")
    
    // 2. 各自插入一条 accounts 记录
    createAccount(tenantA, "1001", "现金")
    createAccount(tenantB, "1001", "Cash")
    
    // 3. 用 TenantA 的 token 设置 tenant = TenantA
    // 4. 查询 accounts（应只看到 TenantA 的）
    results := queryAccounts(tenantA)
    
    if len(results) != 1 {
        t.Errorf("expected 1 account for tenant A, got %d", len(results))
    }
    if results[0].Code != "1001" {
        t.Errorf("expected code 1001, got %s", results[0].Code)
    }
    // 确保不是 TenantB 的 "Cash"
    if results[0].Name == "Cash" {
        t.Error("tenant isolation failed: saw tenant B's data")
    }
}
```

---

## 验收标准

| # | 检查项 | 预期结果 |
|:---|:---|:---|
| 1 | `docker-compose up -d` 启动 | 所有服务 Running |
| 2 | `curl localhost:8080/health` | `{"status":"ok"}` |
| 3 | JWT 无 token 访问受保护路由 | 401 Unauthorized |
| 4 | JWT 无效 token | 401 Unauthorized |
| 5 | RLS 跨租户隔离（集成测试） | Tenant A 看不到 Tenant B 数据 |
| 6 | `go test ./...` | 0 failures |
| 7 | `go build ./cmd/api/` | 编译成功，无错误 |

---

## 关键风险提示

1. **RLS 连接失败**：如果 `SET app.current_tenant` 失败，连接暴露 public schema，必须 fail-fast 返回 500
2. **连接池泄漏**：每个请求用完后必须正确释放连接
3. **JWT Secret 配置**：生产环境必须通过 ENV 或 vault 注入，禁止硬编码

---

## 提交约定

每个 Step 完成后单独 commit：
```bash
git add . && git commit -m "feat: scaffold <step name>"
```

最终 tag：
```bash
git tag -a v0.1.0 -m "feat: TASK-F0.1 project scaffold complete"
```